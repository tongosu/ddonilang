#이름: 수요·공급 기초
#설명: y = 20 - x

(매마디)마다 {
  k <- -1.
  b <- 20.

  x_min <- 0.
  x_max <- 20.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
