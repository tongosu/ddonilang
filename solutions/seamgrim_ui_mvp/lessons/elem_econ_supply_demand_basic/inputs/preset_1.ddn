#이름: elem_econ_supply_demand_basic_variant_수요·공급 기초
#설명: y = 20 - x

k <- -1.
b <- 20.

x_min <- 0.
x_max <- 20.
dx <- 1.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  y <- k * x + b.
  x 보여주기.
  y 보여주기.
}.
