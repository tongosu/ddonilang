#이름: 대칭과 반사
#설명: y = -x

(매마디)마다 {
  k <- -1.
  b <- 0.

  x_min <- -10.
  x_max <- 10.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
