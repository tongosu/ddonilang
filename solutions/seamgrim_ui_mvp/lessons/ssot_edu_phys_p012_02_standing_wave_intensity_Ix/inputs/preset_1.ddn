#이름: ssot_edu_phys_p012_02_standing_wave_intensity_Ix_variant_P012-02 정상파 세기 무늬 I(x)
#설명: 정상파 y(x,t)=2A*sin(kx)*cos(wt)에서 시간에 무관한 세기 무늬를 단순히 I(x)=(2A*sin(kx))^2 로 둔다.
// 출력은 숫자만. (x, I) 순서.

A <- 1.0.
k <- 6.283185307.   // 2*pi

x_min <- 0.
x_max <- 1.
dx <- 0.005.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  th <- (k=k, x=x) (#ascii) 수식{ k*x }.
  s <- ((th) sin).
  amp <- (2*A) * s.
  I <- amp * amp.

  x 보여주기.
  I 보여주기.
}.
