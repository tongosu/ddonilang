// 출력은 숫자만. (x, I) 순서.

A <- 1.0.
k <- 6.283185307.   // 2*pi

x_min <- 0.
x_max <- 1.
dx <- 0.005.

(시작)할때 {
  x <- x_min.
}.

(매마디)마다 {
  { x <= x_max }인것 일때 {
    th <- (k=k, x=x) (#ascii) 수식{ k*x }.
    s <- ((th) sin).
    amp <- (2*A) * s.
    I <- amp * amp.

    x <- x + dx.
  }.
}.
