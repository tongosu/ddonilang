#이름: college_math_series
#설명: y = a x^3 + b x^2

함수 = (#ascii1) 수식{ y = a x^3 + b x^2 }.

a <- 0.1.
b <- -0.4.

x목록 <- (-3, 3, 0.5) 범위.

(x) x목록에 대해: {
  y <- (a=a, b=b, x=x)인 함수 풀기.
  x 보여주기.
  y 보여주기.
}.
