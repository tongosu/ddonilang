#이름: ssot_edu_econ_e010_04_profit_q_pi_variant_E010-04 이윤 π(Q)
#설명: π(Q)=TR(Q)-TC(Q). TR= P(Q)*Q, P(Q)=max(0,a-bQ). 비용 TC=F + cQ + dQ^2. (Q,π) 출력.
a <- 100.
b <- 1.

F <- 200.
c <- 20.
d <- 0.1.

Q_min <- 0.
Q_max <- 140.
dQ <- 1.

Q목록 <- (Q_min, Q_max, dQ) 범위.

(Q) Q목록에 대해: {
  P <- a - (b * Q).
  { P < 0 }인것 일때 {
    P <- 0.
  }.
  TR <- P * Q.
  TC <- F + (c * Q) + (d * Q * Q).
  PI <- TR - TC.

  Q 보여주기.
  PI 보여주기.
}.
