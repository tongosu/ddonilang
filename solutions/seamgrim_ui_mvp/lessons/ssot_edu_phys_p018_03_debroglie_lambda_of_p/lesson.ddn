
(매마디)마다 {
  // 출력은 숫자만. (p, λ) 순서.

  h <- 1.0.

  p_min <- 0.1.
  p_max <- 5.0.
  dp <- 0.05.

  p목록 <- (p_min, p_max, dp) 범위.

  (p) p목록에 대해: {
    lam <- h / p.

    보임 {
      p: p.
      lam: lam.
    }.
  }.
}.
