// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  p <- 0.1.
}.

(매마디)마다 {
  // 출력은 숫자만. (p, λ) 순서.

  h <- 1.0.

  p_min <- 0.1.
  p_max <- 5.0.
  dp <- 0.05.

  { p <= p_max }인것 일때 {
    lam <- h / p.

    보임 {
      p: p.
      lam: lam.
    }.
    p <- p + (dp).
  }.
}.
