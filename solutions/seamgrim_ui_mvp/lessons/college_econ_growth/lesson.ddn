// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  t <- 0.
}.

(매마디)마다 {
  성장 = (#ascii1) 수식{ y = a t + b t^2 }.

  a <- 0.5.
  b <- 0.12.

  { t <= 8 }인것 일때 {
    y <- a t + b t^2.
    보임 {
      t: t.
      y: y.
    }.
    t <- t + (0.5).
  }.
}.
