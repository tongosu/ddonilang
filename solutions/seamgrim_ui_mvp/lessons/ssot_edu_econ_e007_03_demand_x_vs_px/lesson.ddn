
(매마디)마다 {
  // 출력은 숫자만. (px, x*) 순서.

  I <- 100.
  py <- 5.

  px_min <- 1.
  px_max <- 50.
  dpx <- 1.

  px목록 <- (px_min, px_max, dpx) 범위.

  (px) px목록에 대해: {
    x_star <- I / (2*px).

    보임 {
      px: px.
      x_star: x_star.
    }.
  }.
}.
