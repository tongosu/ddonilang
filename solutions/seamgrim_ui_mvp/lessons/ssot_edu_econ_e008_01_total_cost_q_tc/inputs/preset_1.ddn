#이름: ssot_edu_econ_e008_01_total_cost_q_tc_variant_E008-01 총비용 TC(q)
#설명: 총비용 TC(q)=F + c*q + 0.5*d*q^2 를 계산해 (q,TC) 점열 출력.
// 출력은 숫자만. (q, TC) 순서.

F <- 20.
c <- 5.
d <- 0.6.

q_min <- 0.
q_max <- 50.
dq <- 0.5.

q목록 <- (q_min, q_max, dq) 범위.

(q) q목록에 대해: {
  TC <- F + (c*q) + (0.5*d*q*q).

  q 보여주기.
  TC 보여주기.
}.
