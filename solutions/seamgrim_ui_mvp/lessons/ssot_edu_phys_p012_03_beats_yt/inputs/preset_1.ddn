#이름: ssot_edu_phys_p012_03_beats_yt_variant_P012-03 맥놀이(비트) y(t)
#설명: 서로 가까운 두 주파수의 합 y(t)=sin(w1 t)+sin(w2 t). (t,y) 출력.

(매마디)마다 {
  // 출력은 숫자만. (t, y) 순서.

  w1 <- 31.415926535.   // 2*pi*5
  w2 <- 34.557519190.   // 2*pi*5.5

  dt <- 0.001.
  t_max <- 2.0.

  t목록 <- (0, t_max, dt) 범위.

  (t) t목록에 대해: {
    th1 <- (w=w1, t=t) (#ascii) 수식{ w*t }.
    th2 <- (w=w2, t=t) (#ascii) 수식{ w*t }.

    y <- ((th1) sin) + ((th2) sin).

    보임 {
      t: t.
      y: y.
    }.
  }.
}.
