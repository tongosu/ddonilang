#이름: ssot_edu_phys_p015_04_dynamic_pressure_v_q_variant_P015-04 동압 q(v)=½ρv²
#설명: 베르누이에서 등장하는 동압(유체의 속도항) q=½ρv². (v, q) 출력.

(매마디)마다 {
  rho <- 1.2.

  v_min <- 0.
  v_max <- 50.
  dv <- 0.5.

  v목록 <- (v_min, v_max, dv) 범위.

  (v) v목록에 대해: {
    q <- 0.5 * rho * v * v.

    보임 {
      v: v.
      q: q.
    }.
  }.
}.
