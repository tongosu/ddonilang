#이름: P015-04 동압 q(v)=½ρv²
#설명: 베르누이에서 등장하는 동압(유체의 속도항) q=½ρv². (v, q) 출력.
rho <- 1.2.

v_min <- 0.
v_max <- 50.
dv <- 0.5.

v목록 <- (v_min, v_max, dv) 범위.

(v) v목록에 대해: {
  q <- 0.5 * rho * v * v.

  v 보여주기.
  q 보여주기.
}.
