rho <- 1.2.

v_min <- 0.
v_max <- 50.
dv <- 0.5.

(시작)할때 {
  v <- v_min.
}.

(매마디)마다 {
  { v <= v_max }인것 일때 {
    q <- 0.5 * rho * v * v.

    보임 {
      v: v.
      q: q.
    }.
    v <- v + dv.
  }.
}.
