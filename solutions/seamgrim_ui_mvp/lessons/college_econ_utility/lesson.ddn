// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  x <- 0.
}.

(매마디)마다 {
  효용 = (#ascii1) 수식{ y = a x^2 + b x }.

  a <- -0.2.
  b <- 2.4.

  { x <= 10 }인것 일때 {
    y <- a x^2 + b x.
    보임 {
      x: x.
      y: y.
    }.
    x <- x + (1).
  }.
}.
