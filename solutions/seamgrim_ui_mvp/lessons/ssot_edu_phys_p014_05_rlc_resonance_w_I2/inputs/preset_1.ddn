#이름: ssot_edu_phys_p014_05_rlc_resonance_w_I2_variant_P014-05 RLC 공진 I^2(ω)
#설명: 직렬 RLC에서 I(ω)=V0/sqrt(R^2+(ωL-1/(ωC))^2). sqrt를 피하기 위해 I^2(ω)=V0^2/(R^2+(ωL-1/(ωC))^2) 사용. (ω,I2) 출력.

(매마디)마다 {
  V0 <- 1.
  R <- 0.5.
  L <- 1.
  C <- 1.

  w_min <- 0.2.
  w_max <- 4.
  dw <- 0.02.

  w목록 <- (w_min, w_max, dw) 범위.

  (w) w목록에 대해: {
    X <- (w * L) - (1 / (w * C)).
    denom <- (R * R) + (X * X).
    I2 <- (V0 * V0) / denom.

    보임 {
      w: w.
      I2: I2.
    }.
  }.
}.
