
(매마디)마다 {
  V0 <- 1.
  R <- 0.5.
  L <- 1.
  C <- 1.

  w_min <- 0.2.
  w_max <- 4.
  dw <- 0.02.

  w목록 <- (w_min, w_max, dw) 범위.

  (w) w목록에 대해: {
    X <- (w * L) - (1 / (w * C)).
    denom <- (R * R) + (X * X).
    I2 <- (V0 * V0) / denom.

    보임 {
      w: w.
      I2: I2.
    }.
  }.
}.
