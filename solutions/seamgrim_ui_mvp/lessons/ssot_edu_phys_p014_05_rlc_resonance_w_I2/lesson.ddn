V0 <- 1.
R <- 0.5.
L <- 1.
C <- 1.

w_min <- 0.2.
w_max <- 4.
dw <- 0.02.

(시작)할때 {
  w <- w_min.
}.

(매마디)마다 {
  { w <= w_max }인것 일때 {
    X <- (w * L) - (1 / (w * C)).
    denom <- (R * R) + (X * X).
    I2 <- (V0 * V0) / denom.

    보임 {
      w: w.
      I2: I2.
    }.
    w <- w + dw.
  }.
}.
