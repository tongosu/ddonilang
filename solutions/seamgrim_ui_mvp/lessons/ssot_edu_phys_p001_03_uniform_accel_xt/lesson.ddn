(매마디)마다 {
  // 출력은 숫자만. (t, x) 순서.

  x0 <- 0.
  v0 <- 0.
  a <- 1.
  dt <- 0.1.
  t_max <- 10.

  t <- 0.

  { t <= t_max }인것 동안: {
    x <- (x0=x0, v0=v0, a=a, t=t) (#ascii) 수식{ x0 + v0*t + (a*t^2)/2 }.

    // (t, x)
    보임 {
      값1: t.
      값2: x.
    }.

    t <- t + dt.
  }.
}.
