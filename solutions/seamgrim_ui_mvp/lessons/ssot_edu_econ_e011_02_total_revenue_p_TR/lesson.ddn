
(매마디)마다 {
  a <- 100.
  b <- 1.

  P_min <- 0.
  P_max <- 120.
  dP <- 1.

  P목록 <- (P_min, P_max, dP) 범위.

  (P) P목록에 대해: {
    Q <- a - (b * P).
    { Q < 0 }인것 일때 {
      Q <- 0.
    }.
    TR <- P * Q.

    보임 {
      P: P.
      TR: TR.
    }.
  }.
}.
