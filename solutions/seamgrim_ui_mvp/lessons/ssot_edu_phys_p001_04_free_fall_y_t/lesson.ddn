#이름: P001-04 자유낙하 y-t
#설명: 자유낙하: y(t)=y0-(g*t^2)/2 (출력: t y, y<0 되면 종료)

(매마디)마다 {
  // 출력은 숫자만. (t, y) 순서.
  // NOTE: y<0(지면 아래)면 출력하지 않고 종료한다.

  살림.y0 <- 50.
  살림.g <- 9.8.
  살림.dt <- 0.05.

  살림.t <- 0.

  반복: {
    살림.y <- (y0=살림.y0, g=살림.g, t=살림.t) (#ascii) 수식{ y0 - (g*t^2)/2 }.

    { 살림.y < 0 }인것 일때 {
      멈추기.
    }.

    // (t, y)
    보임 {
      값1: 살림.t.
      값2: 살림.y.
    }.

    살림.t <- 살림.t + 살림.dt.
  }.
}.
