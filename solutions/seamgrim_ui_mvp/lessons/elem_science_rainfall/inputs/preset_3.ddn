#이름: 월별 강수량
#설명: 월별 패턴

(매마디)마다 {
  A <- 5.
  w <- 0.6.

  x_min <- 1.
  x_max <- 12.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    angle <- w * x.
    s <- (angle) sin.
    y <- A * s.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
