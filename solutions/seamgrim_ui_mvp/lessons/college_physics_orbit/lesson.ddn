#이름: college_physics_orbit
#설명: 단순 궤적 근사

(매마디)마다 {
  수평 = (#ascii1) 수식{ x = v t }.
  수직 = (#ascii1) 수식{ y = u t - 0.5 g t^2 }.

  v <- 3.
  u <- 5.
  g <- 1.2.

  t목록 <- (0, 4, 0.4) 범위.

  (t) t목록에 대해: {
    x <- (v=v, t=t)인 수평 풀기.
    y <- (u=u, g=g, t=t)인 수직 풀기.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
