#이름: elem_science_moon_phase_variant_달의 위상
#설명: sin 곡선

A <- 1.
w <- 1.

x_min <- 0.
x_max <- 10.
dx <- 1.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  angle <- w * x.
  s <- (angle) sin.
  y <- A * s.
  x 보여주기.
  y 보여주기.
}.
