#이름: elem_math_percentage_of_variant_비율 계산(%)
#설명: y = x * 1/4

(매마디)마다 {
  x_min <- 0.0.
  x_max <- 100.0.
  dx <- 10.0.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- x * 1 / 4.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
