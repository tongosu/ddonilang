#이름: lesson_math_line
#설명: y = a*x + b 직선 (상태 기반 시뮬)
#control: a:수=2 [-5..5] step=0.1; b:수=1 [-10..10] step=0.1; x_start:수=-4 [-20..20] step=0.5; x_end:수=4 [-20..20] step=0.5; dx:수=0.2 [0.05..2] step=0.05

(매마디)마다 {
  a <- 2.
  b <- 1.
  x_start <- -4.
  x_end <- 4.
  dx <- 0.2.

  x <- x_start.
  { x <= x_end }인것 동안: {
    y <- a * x + b.
    보임 {
      x: x.
      y: y.
    }.
    x <- x + dx.
  }.
}.
