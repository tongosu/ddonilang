#이름: E001-01 수요곡선 P(Q)
#설명: Q = a - bP 를 P = (a - Q)/b 로 바꿔 (Q,P) 점열 출력.

// 출력은 숫자만. (Q, P) 순서.
a <- 100.
b <- 2.

Q_min <- 0.
Q_max <- 100.
dQ <- 2.

수요_PQ = (#ascii1) 수식{ y = (a - x) / b }.
Q목록 <- (Q_min, Q_max, dQ) 범위.

(x) Q목록에 대해: {
  P <- (x=x, a=a, b=b)인 수요_PQ 풀기.
  x 보여주기.
  P 보여주기.
}.
