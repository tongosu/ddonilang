#이름: 퍼센트 감각
#설명: y = 0.01x

(매마디)마다 {
  k <- 0.01.
  b <- 0.

  x_min <- 0.
  x_max <- 102.
  dx <- 10.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
