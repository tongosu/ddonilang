
(매마디)마다 {
  // 출력은 숫자만. (d_o, d_i) 순서.

  f <- 10.

  d_o_min <- 12.
  d_o_max <- 100.
  dd_o <- 2.

  d_i_of_d_o = (#ascii1) 수식{ y = (f * x) / (x - f) }.
  d_o목록 <- (d_o_min, d_o_max, dd_o) 범위.

  (x) d_o목록에 대해: {
    d_i <- (x=x, f=f)인 d_i_of_d_o 풀기.
    보임 {
      x: x.
      d_i: d_i.
    }.
  }.
}.
