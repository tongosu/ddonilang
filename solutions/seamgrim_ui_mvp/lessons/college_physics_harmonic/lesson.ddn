#이름: college_physics_harmonic
#설명: y = a t^2 - b t

운동 = (#ascii1) 수식{ y = a t^2 - b t }.

a <- 0.25.
b <- 1.2.

t목록 <- (0, 6, 0.3) 범위.

(t) t목록에 대해: {
  y <- (a=a, b=b, t=t)인 운동 풀기.
  t 보여주기.
  y 보여주기.
}.
