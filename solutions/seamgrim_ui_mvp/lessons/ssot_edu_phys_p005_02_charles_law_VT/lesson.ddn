#이름: P005-02 기체 법칙(정압) V-T
#설명: V = n R T / P  (출력: T V)

// 출력은 숫자만. (T, V) 순서.
// NOTE: export_graph.py는 stdout 숫자쌍을 (x,y)로 묶는다.

n <- 1.
R <- 8.314.
P <- 101.325.

T_min <- 250.
T_max <- 400.
dT <- 10.

부피식 = (#ascii1) 수식{ y = n*R*x / P }.

T목록 <- (T_min, T_max, dT) 범위.

(T) T목록에 대해: {
  V <- (n=n, R=R, P=P, x=T)인 부피식 풀기.
  T 보여주기.
  V 보여주기.
}.
