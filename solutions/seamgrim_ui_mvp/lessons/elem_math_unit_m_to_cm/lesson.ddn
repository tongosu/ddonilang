#이름: m→cm 변환
#설명: y = 100x

(매마디)마다 {
  k <- 100.
  b <- 0.

  x_min <- 0.
  x_max <- 10.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
