#이름: elem_math_arithmetic_sequence_variant_등차수열
#설명: y = 2x + 1

(매마디)마다 {
  k <- 2.
  b <- 1.

  x_min <- 0.
  x_max <- 10.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
