#이름: 진자 운동
#설명: sin 곡선

(매마디)마다 {
  A <- 5.
  w <- 0.8.

  x_min <- 0.
  x_max <- 12.
  dx <- 0.5.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    angle <- w * x.
    s <- (angle) sin.
    y <- A * s.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
