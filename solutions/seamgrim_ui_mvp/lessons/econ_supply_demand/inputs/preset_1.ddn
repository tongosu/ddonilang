#이름: econ_supply_demand_variant_econ_supply_demand
#설명: 수요 곡선(예시)

수요 = (#ascii1) 수식{ y = a x + b }.

a <- -1.
b <- 10.

x목록 <- (0, 10, 1) 범위.

(x) x목록에 대해: {
  y <- (a=a, b=b, x=x)인 수요 풀기.
  x 보여주기.
  y 보여주기.
}.
