
(매마디)마다 {
  // 출력은 숫자만. (t, N) 순서.

  N0 <- 1000.
  lambda <- 0.05.
  dt <- 1.

  t_max <- 100.
  t목록 <- (0, t_max, dt) 범위.

  N <- N0.

  (t) t목록에 대해: {
    보임 {
      t: t.
      N: N.
    }.

    N <- N * (1 - lambda*dt).
    { N < 0 }인것 일때 {
      N <- 0.
    }.
  }.
}.
