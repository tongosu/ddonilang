// 출력은 숫자만. (t, N) 순서.

N0 <- 1000.
lambda <- 0.05.
dt <- 1.

t_max <- 100.

(시작)할때 {
  t <- 0.
  N <- N0.
}.

(매마디)마다 {
  { t <= t_max }인것 일때 {

    N <- N * (1 - lambda*dt).
    { N < 0 }인것 일때 {
      N <- 0.
    }.
    t <- t + dt.
  }.
}.
