(매마디)마다 {
  // 출력은 숫자만. (t, v) 순서.

  v0 <- 0.
  a <- 1.5.
  dt <- 0.1.
  t_max <- 10.

  t <- 0.

  { t <= t_max }인것 동안: {
    v <- (v0=v0, a=a, t=t) (#ascii) 수식{ v0 + a*t }.

    // (t, v)

    t <- t + dt.
  }.
}.
