#이름: P001-02 등가속도운동 v-t
#설명: 등가속도: v(t)=v0+a*t (출력: t v)

// 출력은 숫자만. (t, v) 순서.

살림.v0 <- 0.
살림.a <- 1.5.
살림.dt <- 0.1.
살림.t_max <- 10.

살림.t <- 0.

{ 살림.t <= 살림.t_max }인것 동안: {
  살림.v <- (v0=살림.v0, a=살림.a, t=살림.t) (#ascii) 수식{ v0 + a*t }.

  // (t, v)
  살림.t 보여주기.
  살림.v 보여주기.

  살림.t <- 살림.t + 살림.dt.
}.
