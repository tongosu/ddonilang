#이름: 수요 감소
#설명: y = 30 - x

(매마디)마다 {
  k <- -1.
  b <- 30.

  x_min <- 0.
  x_max <- 20.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
