#이름: college_math_linear
#설명: y = a x + b

(매마디)마다 {
  함수 = (#ascii1) 수식{ y = a x + b }.

  a <- 1.5.
  b <- -1.

  x목록 <- (-2, 4, 0.5) 범위.

  (x) x목록에 대해: {
    y <- (a=a, b=b, x=x)인 함수 풀기.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
