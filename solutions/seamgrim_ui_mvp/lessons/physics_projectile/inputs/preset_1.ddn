
// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  t <- 0.
}.

(매마디)마다 {
  수평 = (#ascii1) 수식{ x = v t }.
  수직 = (#ascii1) 수식{ y = u t - 0.5 g t^2 }.

  v <- 4.
  u <- 8.
  g <- 9.8.

  { t <= 1.6 }인것 일때 {
    x <- v t.
    y <- u t - 0.5 g t^2.
    보임 {
      x: x.
      y: y.
    }.
    t <- t + (0.2).
  }.
}.
