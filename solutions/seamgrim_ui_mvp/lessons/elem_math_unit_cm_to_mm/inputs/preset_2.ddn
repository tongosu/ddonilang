#이름: cm→mm 변환
#설명: y = 10x

(매마디)마다 {
  k <- 10.
  b <- 0.

  x_min <- 0.
  x_max <- 22.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
