#이름: 설문 결과
#설명: y = 2x + 3

(매마디)마다 {
  k <- 2.
  b <- 3.

  x_min <- 1.
  x_max <- 6.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
