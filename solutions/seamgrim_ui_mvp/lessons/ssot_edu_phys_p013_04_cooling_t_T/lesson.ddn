
(매마디)마다 {
  k <- 0.15.
  T_env <- 20.
  T <- 100.

  t <- 0.
  t_max <- 60.
  dt <- 0.5.

  { t <= t_max }인것 동안: {
    dT <- (-k) * (T - T_env).
    T <- T + (dT * dt).

    보임 {
      t: t.
      T: T.
    }.

    t <- t + dt.
  }.
}.
