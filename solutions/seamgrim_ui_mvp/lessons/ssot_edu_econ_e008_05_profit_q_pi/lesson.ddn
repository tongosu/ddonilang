
(매마디)마다 {
  // 출력은 숫자만. (q, pi) 순서.

  p <- 20.

  F <- 20.
  c <- 5.
  d <- 0.6.

  q_min <- 0.
  q_max <- 50.
  dq <- 0.5.

  q목록 <- (q_min, q_max, dq) 범위.

  (q) q목록에 대해: {
    TC <- F + (c*q) + (0.5*d*q*q).
    pi <- (p*q) - TC.

    보임 {
      q: q.
      pi: pi.
    }.
  }.
}.
