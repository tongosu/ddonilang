#이름: ssot_edu_econ_e008_05_profit_q_pi_variant_E008-05 이윤 π(q) (가격 p 고정)
#설명: π(q)=p*q - TC(q), TC(q)=F + c*q + 0.5*d*q^2. (q, π) 출력.
// 출력은 숫자만. (q, pi) 순서.

p <- 20.

F <- 20.
c <- 5.
d <- 0.6.

q_min <- 0.
q_max <- 50.
dq <- 0.5.

q목록 <- (q_min, q_max, dq) 범위.

(q) q목록에 대해: {
  TC <- F + (c*q) + (0.5*d*q*q).
  pi <- (p*q) - TC.

  q 보여주기.
  pi 보여주기.
}.
