#이름: elem_econ_allowance_budget_variant_용돈과 지출
#설명: y = 50 - 3x

k <- -3.
b <- 50.

x_min <- 0.
x_max <- 12.
dx <- 1.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  y <- k * x + b.
  x 보여주기.
  y 보여주기.
}.
