#이름: 용돈과 지출
#설명: y = 50 - 3x

(매마디)마다 {
  k <- -3.
  b <- 50.

  x_min <- 0.
  x_max <- 12.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
