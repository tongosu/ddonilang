#이름: ssot_edu_econ_e013_03_ppf_linear_xy_variant_E013-03 생산가능곡선 PPF(직선형)
#설명: Y = A - b*X.  (교환관계/기회비용 직관)

(매마디)마다 {
  // 출력은 숫자만. (X, Y) 순서.

  A <- 100.
  b <- 1.5.

  X_min <- 0.
  X_max <- 60.
  dX <- 1.

  X목록 <- (X_min, X_max, dX) 범위.

  (X) X목록에 대해: {
    Y <- A - b*X.
    { Y < 0 }인것 일때 {
      Y <- 0.
    }.
    보임 {
      X: X.
      Y: Y.
    }.
  }.
}.
