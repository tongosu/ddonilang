
// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  x <- 0.
}.

(매마디)마다 {
  함수 = (#ascii1) 수식{ y = 2 x }.

  { x <= 4 }인것 일때 {
    y <- 2 x.
    보임 {
      x: x.
      y: y.
    }.
    x <- x + (1).
  }.
}.
