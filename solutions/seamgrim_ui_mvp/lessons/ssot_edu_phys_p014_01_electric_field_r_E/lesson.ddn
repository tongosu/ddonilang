#이름: P014-01 전기장 E(r)
#설명: 점전하 주변 전기장 크기 E(r)=k/r^2 (교육용 스케일). r=0 회피. (r,E) 출력.
k <- 1000.

r_min <- 1.
r_max <- 50.
dr <- 1.

r목록 <- (r_min, r_max, dr) 범위.

(r) r목록에 대해: {
  E <- k / (r * r).

  r 보여주기.
  E 보여주기.
}.
