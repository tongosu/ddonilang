#이름: P014-03 RC 방전 V(t)
#설명: RC 방전: dV/dt = -(1/(R*C)) V 를 이산 업데이트로 근사. V <- V - (dt/(R*C))*V. (t,V) 출력.

(매마디)마다 {
  R <- 1.
  C <- 1.

  dt <- 0.05.
  t_max <- 5.

  V <- 1.

  t목록 <- (0, t_max, dt) 범위.

  (t) t목록에 대해: {
    보임 {
      t: t.
      V: V.
    }.

    V <- V - ((dt / (R * C)) * V).
    { V < 0 }인것 일때 {
      V <- 0.
    }.
  }.
}.
