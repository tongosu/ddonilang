#이름: ssot_edu_econ_e011_01_price_elasticity_p_absE_variant_E011-01 가격탄력성 |e(P)| (선형수요)
#설명: 선형수요 Q(P)=a-bP에서 가격탄력성의 크기 |e|=(bP)/(a-bP). (P, |e|) 점열 출력.

(매마디)마다 {
  a <- 100.
  b <- 1.

  P_min <- 1.
  P_max <- 99.
  dP <- 1.

  P목록 <- (P_min, P_max, dP) 범위.

  (P) P목록에 대해: {
    Q <- a - (b * P).
    # 선형수요 Q(P)=a-bP 에서 가격탄력성 e = (dQ/dP)*(P/Q) = (-b)*(P/Q).
    # 여기서는 크기(|e|)만 표시: |e| = (b*P)/Q.
    absE <- (b * P) / Q.

    보임 {
      P: P.
      absE: absE.
    }.
  }.
}.
