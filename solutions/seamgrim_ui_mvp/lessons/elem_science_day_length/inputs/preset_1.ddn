#이름: elem_science_day_length_variant_하루 낮의 길이
#설명: sin 곡선

A <- 2.
w <- 0.5.

x_min <- 0.
x_max <- 12.
dx <- 1.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  angle <- w * x.
  s <- (angle) sin.
  y <- A * s.
  x 보여주기.
  y 보여주기.
}.
