#이름: ssot_edu_phys_p005_05_newton_cooling_Tt_discrete_variant_P005-05 뉴턴 냉각(이산) T-t
#설명: T_next = T - k*(T - Tenv)*dt  (출력: t T)

// 출력은 숫자만. (t, T) 순서.
// NOTE: export_graph.py는 stdout 숫자쌍을 (x,y)로 묶는다.

// 이 수업은 '지수함수(exp)'를 직접 쓰지 않고,
// 작은 dt로 반복 업데이트해서 냉각 곡선을 만든다(이산 근사).

Tenv <- 20.
T <- 90.
k <- 0.15.
dt <- 0.5.
t_max <- 30.

t목록 <- (0, t_max, dt) 범위.

(t) t목록에 대해: {
  t 보여주기.
  T 보여주기.

  T <- T - k * (T - Tenv) * dt.
}.
