#이름: E014-05 베버리지 곡선 v(u)
#설명: v = max(0, v0 - alpha*u).  (실업↑ → 공석↓)  간이 직선형.
// 출력은 숫자만. (u, v) 순서.

v0 <- 0.15.
alpha <- 0.60.

u_min <- 0.00.
u_max <- 0.25.
du <- 0.005.

u목록 <- (u_min, u_max, du) 범위.

(u) u목록에 대해: {
  v <- v0 - alpha*u.
  { v < 0 }인것 일때 {
    v <- 0.
  }.
  u 보여주기.
  v 보여주기.
}.
