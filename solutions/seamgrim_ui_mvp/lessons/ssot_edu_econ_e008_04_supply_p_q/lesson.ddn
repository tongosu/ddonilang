#      p < c 이면 생산 0으로 둔다. (p,q) 출력.

(매마디)마다 {
  // 출력은 숫자만. (p, q) 순서.

  c <- 5.
  d <- 0.6.

  p_min <- 0.
  p_max <- 40.
  dp <- 0.5.

  p목록 <- (p_min, p_max, dp) 범위.

  (p) p목록에 대해: {
    q <- (p - c) / d.

    // p < c 이면 q=0 (단순 셧다운 규칙)
    고르기:
      { (q < 0) }인것: { q <- 0. }
      아니면: { q <- q. }
    보임 {
      p: p.
      q: q.
    }.
  }.
}.
