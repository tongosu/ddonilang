
(매마디)마다 {
  b <- 1.
  c <- 20.

  a_min <- 0.
  a_max <- 200.
  da <- 2.

  a목록 <- (a_min, a_max, da) 범위.

  (a) a목록에 대해: {
    Qm <- (a - c) / (2 * b).
    { Qm < 0 }인것 일때 {
      Qm <- 0.
    }.
    보임 {
      a: a.
      Qm: Qm.
    }.
  }.
}.
