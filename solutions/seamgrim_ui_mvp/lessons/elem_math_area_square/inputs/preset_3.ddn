#이름: 정사각형 넓이
#설명: y = x^2

(매마디)마다 {
  a <- 1.
  h <- 0.
  k <- 0.

  x_min <- 0.
  x_max <- 10.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- a * (x - h) * (x - h) + k.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
