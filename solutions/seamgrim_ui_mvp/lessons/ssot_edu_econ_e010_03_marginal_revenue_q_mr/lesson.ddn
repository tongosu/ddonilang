
(매마디)마다 {
  a <- 100.
  b <- 1.

  Q_min <- 0.
  Q_max <- 140.
  dQ <- 1.

  Q목록 <- (Q_min, Q_max, dQ) 범위.

  (Q) Q목록에 대해: {
    MR <- a - (2 * b * Q).
    보임 {
      Q: Q.
      MR: MR.
    }.
  }.
}.
