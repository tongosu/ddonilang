
(매마디)마다 {
  // 출력은 숫자만. (f, KE) 순서.

  h <- 1.
  f0 <- 5.

  f_min <- 0.
  f_max <- 10.
  df <- 0.25.

  KE_linear = (#ascii1) 수식{ y = h * (x - f0) }.
  f목록 <- (f_min, f_max, df) 범위.

  (x) f목록에 대해: {
    KE <- 0.
    { x >= f0 }인것 일때 {
      KE <- (x=x, h=h, f0=f0)인 KE_linear 풀기.
    }.
    보임 {
      x: x.
      KE: KE.
    }.
  }.
}.
