
(매마디)마다 {
  // 출력은 숫자만. (n, E) 순서.

  E0 <- 13.6.

  n_min <- 1.
  n_max <- 10.
  dn <- 1.

  n목록 <- (n_min, n_max, dn) 범위.

  (n) n목록에 대해: {
    n2 <- n * n.
    E <- -E0 / n2.

    보임 {
      n: n.
      E: E.
    }.
  }.
}.
