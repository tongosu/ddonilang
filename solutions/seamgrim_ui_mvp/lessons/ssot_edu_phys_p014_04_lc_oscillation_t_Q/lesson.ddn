
(매마디)마다 {
  L <- 1.
  C <- 1.

  dt <- 0.02.
  t_max <- 10.

  Q <- 1.
  I <- 0.

  t목록 <- (0, t_max, dt) 범위.

  (t) t목록에 대해: {
    보임 {
      t: t.
      Q: Q.
    }.

    I <- I + (-(Q / (L * C)) * dt).
    Q <- Q + (I * dt).
  }.
}.
