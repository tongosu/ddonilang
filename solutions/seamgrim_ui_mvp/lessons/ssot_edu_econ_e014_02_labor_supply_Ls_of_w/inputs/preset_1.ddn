#이름: ssot_edu_econ_e014_02_labor_supply_Ls_of_w_variant_E014-02 노동공급 Ls(w)
#설명: Ls = max(0, c + d*w).  (임금↑ → 공급↑)

(매마디)마다 {
  // 출력은 숫자만. (w, Ls) 순서.

  c <- 10.
  d <- 1.5.

  w_min <- 0.
  w_max <- 60.
  dw <- 1.

  w목록 <- (w_min, w_max, dw) 범위.

  (w) w목록에 대해: {
    Ls <- c + d*w.
    { Ls < 0 }인것 일때 {
      Ls <- 0.
    }.
    보임 {
      w: w.
      Ls: Ls.
    }.
  }.
}.
