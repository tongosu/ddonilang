#이름: 계절과 기온
#설명: sin 곡선으로 기온 변화를 표현

(매마디)마다 {
  A <- 10.
  w <- 0.5.

  x_min <- 0.
  x_max <- 12.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    angle <- w * x.
    s <- (angle) sin.
    y <- A * s.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
