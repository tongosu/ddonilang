
// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  x <- -3.
}.

(매마디)마다 {
  함수 = (#ascii1) 수식{ y = x^3 - 3 x }.

  { x <= 3 }인것 일때 {
    y <- x^3 - 3 x.
    보임 {
      x: x.
      y: y.
    }.
    x <- x + (0.5).
  }.
}.
