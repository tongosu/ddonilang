#이름: ssot_edu_econ_e001_05_elasticity_along_demand_variant_E001-05 수요 탄력성 |ε|(Q)
#설명: 선형 수요 Q=a-bP (b>0)에서 가격탄력성 크기 |ε| = b*(P/Q) = (a-Q)/Q.
// 출력은 숫자만. (Q, |ε|) 순서.

a <- 100.

Q_min <- 2.
Q_max <- 100.
dQ <- 2.

탄력식 = (#ascii1) 수식{ y = (a - x) / x }.
Q목록 <- (Q_min, Q_max, dQ) 범위.

(x) Q목록에 대해: {
  e <- (x=x, a=a)인 탄력식 풀기.
  x 보여주기.
  e 보여주기.
}.
