
(매마디)마다 {
  // 출력은 숫자만. (Q, |ε|) 순서.

  a <- 100.

  Q_min <- 2.
  Q_max <- 100.
  dQ <- 2.

  탄력식 = (#ascii1) 수식{ y = (a - x) / x }.
  Q목록 <- (Q_min, Q_max, dQ) 범위.

  (x) Q목록에 대해: {
    e <- (x=x, a=a)인 탄력식 풀기.
    보임 {
      x: x.
      e: e.
    }.
  }.
}.
