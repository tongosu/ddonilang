// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  t <- 0.
}.

(매마디)마다 {
  이자 = (#ascii1) 수식{ y = p + r t }.

  p <- 1.
  r <- 0.08.

  { t <= 10 }인것 일때 {
    y <- p + r t.
    보임 {
      t: t.
      y: y.
    }.
    t <- t + (1).
  }.
}.
