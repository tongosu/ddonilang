#이름: P002-03 위치에너지 U-h
#설명: 위치에너지: U(h)=m*g*h (출력: h U)

// 출력은 숫자만. (h, U) 순서.

살림.m <- 1.
살림.g <- 9.8.
살림.dh <- 0.2.
살림.h_max <- 20.

살림.h <- 0.

{ 살림.h <= 살림.h_max }인것 동안: {
  살림.U <- (m=살림.m, g=살림.g, h=살림.h) (#ascii) 수식{ m*g*h }.

  살림.h 보여주기.
  살림.U 보여주기.

  살림.h <- 살림.h + 살림.dh.
}.
