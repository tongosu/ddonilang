
(매마디)마다 {
  // 출력은 숫자만. (d, Δy) 순서.

  K <- 10.  // K = λ*L (단위는 생략)

  d_min <- 1.
  d_max <- 20.
  dd <- 1.

  dy_of_d = (#ascii1) 수식{ y = K / x }.
  d목록 <- (d_min, d_max, dd) 범위.

  (x) d목록에 대해: {
    dy <- (x=x, K=K)인 dy_of_d 풀기.
    보임 {
      x: x.
      dy: dy.
    }.
  }.
}.
