// 출력은 숫자만. (d, Δy) 순서.

K <- 10.  // K = λ*L (단위는 생략)

d_min <- 1.
d_max <- 20.
dd <- 1.

dy_of_d = (#ascii1) 수식{ y = K / x }.

(시작)할때 {
  d <- d_min.
}.

(매마디)마다 {
  { d <= d_max }인것 일때 {
    dy <- K / d.
    d <- d + dd.
  }.
}.
