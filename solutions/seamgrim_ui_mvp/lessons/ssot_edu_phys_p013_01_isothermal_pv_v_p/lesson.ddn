#이름: P013-01 등온 과정 PV 곡선 P(V)=k/V
#설명: 등온 과정에서 P(V)=k/V. (V,P) 점열 출력.

(매마디)마다 {
  k <- 100.

  V_min <- 1.
  V_max <- 20.
  dV <- 0.1.

  V목록 <- (V_min, V_max, dV) 범위.

  (V) V목록에 대해: {
    P <- k / V.

    보임 {
      V: V.
      P: P.
    }.
  }.
}.
