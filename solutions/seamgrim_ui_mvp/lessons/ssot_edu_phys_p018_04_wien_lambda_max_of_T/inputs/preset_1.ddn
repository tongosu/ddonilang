// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  T <- 1.0.
}.

(매마디)마다 {
  // 출력은 숫자만. (T, λ_max) 순서.

  b <- 2.9.

  T_min <- 1.0.
  T_max <- 10.0.
  dT <- 0.1.

  { T <= T_max }인것 일때 {
    lam <- b / T.

    T <- T + (dT).
  }.
}.
