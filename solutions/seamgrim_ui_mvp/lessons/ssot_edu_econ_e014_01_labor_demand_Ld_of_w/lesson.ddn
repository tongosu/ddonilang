#이름: E014-01 노동수요 Ld(w)
#설명: Ld = max(0, a - b*w).  (임금↑ → 수요↓)
// 출력은 숫자만. (w, Ld) 순서.

a <- 100.
b <- 2.

w_min <- 0.
w_max <- 60.
dw <- 1.

w목록 <- (w_min, w_max, dw) 범위.

(w) w목록에 대해: {
  Ld <- a - b*w.
  { Ld < 0 }인것 일때 {
    Ld <- 0.
  }.
  w 보여주기.
  Ld 보여주기.
}.
