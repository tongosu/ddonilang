
(매마디)마다 {
  // 출력은 숫자만. (Q*, P*) 순서.
  a <- 100.
  b <- 2.
  c <- 0.
  d <- 1.

  // Q* = (d*a + b*c) / (b + d)
  Q별 = (#ascii1) 수식{ y = (d*a + b*c) / (b + d) }.
  P수요 = (#ascii1) 수식{ y = (a - x) / b }.

  Qstar <- (a=a, b=b, c=c, d=d)인 Q별 풀기.
  Pstar <- (x=Qstar, a=a, b=b)인 P수요 풀기.

  보임 {
    Qstar: Qstar.
    Pstar: Pstar.
  }.
}.
