#이름: 짝수 패턴
#설명: y = 2x

(매마디)마다 {
  k <- 2.
  b <- 0.

  x_min <- 0.
  x_max <- 10.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
