#이름: 공급 증가
#설명: y = x + 5

(매마디)마다 {
  k <- 1.
  b <- 5.

  x_min <- 0.
  x_max <- 20.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
