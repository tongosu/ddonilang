
(매마디)마다 {
  pi <- 3.141592.
  g <- 9.8.

  L_min <- 0.
  L_max <- 4.
  dL <- 0.05.

  coef <- (4 * pi * pi) / g.  # T^2 = coef * L

  L목록 <- (L_min, L_max, dL) 범위.

  (L) L목록에 대해: {
    T2 <- coef * L.

    보임 {
      L: L.
      T2: T2.
    }.
  }.
}.
