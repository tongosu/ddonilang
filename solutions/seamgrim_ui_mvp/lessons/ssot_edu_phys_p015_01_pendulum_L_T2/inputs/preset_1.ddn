pi <- 3.141592.
g <- 9.8.

L_min <- 0.
L_max <- 4.
dL <- 0.05.

coef <- (4 * pi * pi) / g.  # T^2 = coef * L

(시작)할때 {
  L <- L_min.
}.

(매마디)마다 {
  { L <= L_max }인것 일때 {
    T2 <- coef * L.

    L <- L + dL.
  }.
}.
