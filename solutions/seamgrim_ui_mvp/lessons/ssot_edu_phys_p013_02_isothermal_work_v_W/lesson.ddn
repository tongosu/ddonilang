
(매마디)마다 {
  k <- 100.

  V_min <- 1.
  V_max <- 20.
  dV <- 0.1.

  W <- 0.

  V목록 <- (V_min, V_max, dV) 범위.

  (V) V목록에 대해: {
    P <- k / V.
    W <- W + (P * dV).

    보임 {
      V: V.
      W: W.
    }.
  }.
}.
