#이름: P013-02 등온 팽창에서 누적 일 W(V) (수치적분)
#설명: W(V)=∫ P dV 를 누적합으로 근사. P(V)=k/V. (V,W) 점열 출력.

(매마디)마다 {
  k <- 100.

  V_min <- 1.
  V_max <- 20.
  dV <- 0.1.

  W <- 0.

  V목록 <- (V_min, V_max, dV) 범위.

  (V) V목록에 대해: {
    P <- k / V.
    W <- W + (P * dV).

    보임 {
      V: V.
      W: W.
    }.
  }.
}.
