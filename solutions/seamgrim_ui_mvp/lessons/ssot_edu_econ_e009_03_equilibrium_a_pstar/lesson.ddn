
(매마디)마다 {
  b <- 2.
  c <- -20.
  d <- 1.5.

  a_min <- 60.
  a_max <- 140.
  da <- 2.

  a목록 <- (a_min, a_max, da) 범위.

  (a) a목록에 대해: {
    분모 <- b + d.
    p별 <- (a - c) / 분모.
    { p별 < 0 }인것 일때 {
      p별 <- 0.
    }.
    보임 {
      a: a.
      p별: p별.
    }.
  }.
}.
