#이름: ssot_edu_econ_e009_03_equilibrium_a_pstar_variant_E009-03 수요 절편 a 변화에 따른 균형가격 p*
#설명: qd(p)=a-bp, qs(p)=c+dp에서 균형가격 p*를 a에 대해 스윕하여 (a,p*) 출력.

b <- 2.
c <- -20.
d <- 1.5.

a_min <- 60.
a_max <- 140.
da <- 2.

a목록 <- (a_min, a_max, da) 범위.

(a) a목록에 대해: {
  분모 <- b + d.
  p별 <- (a - c) / 분모.
  { p별 < 0 }인것 일때 {
    p별 <- 0.
  }.
  a 보여주기.
  p별 보여주기.
}.
