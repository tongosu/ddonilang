#이름: high_math_polynomial_variant_high_math_polynomial
#설명: y = a x^3 + b x

함수 = (#ascii1) 수식{ y = a x^3 + b x }.

a <- 0.2.
b <- -1.2.

x목록 <- (-3, 3, 0.5) 범위.

(x) x목록에 대해: {
  y <- (a=a, b=b, x=x)인 함수 풀기.
  x 보여주기.
  y 보여주기.
}.
