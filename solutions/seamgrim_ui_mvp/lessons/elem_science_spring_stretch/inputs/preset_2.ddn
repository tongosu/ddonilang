
// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  x <- 0.0.
}.

(매마디)마다 {
  x_min <- 0.0.
  x_max <- 12.0.
  dx <- 1.0.

  { x <= x_max }인것 일때 {
    y <- x * 1 / 2.
    보임 {
      x: x.
      y: y.
    }.
    x <- x + (dx).
  }.
}.
