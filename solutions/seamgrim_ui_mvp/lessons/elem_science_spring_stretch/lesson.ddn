#이름: 스프링 늘어남
#설명: y = 0.5x

(매마디)마다 {
  x_min <- 0.0.
  x_max <- 10.0.
  dx <- 1.0.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- x * 1 / 2.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
