# Seamgrim Lessons

This folder is the active lesson track for the current Seamgrim UI.

The loader reads this track through:

- `active_allowlist.detjson`
- `index.json`
- per-lesson files under this folder

Do not merge this folder with `lessons_rewrite_v1` unless the loader and
representative lesson evidence are updated in the same change.
