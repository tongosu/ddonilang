#이름: college_econ_productivity
#설명: y = a x^2 + b

생산 = (#ascii1) 수식{ y = a x^2 + b }.

a <- 0.1.
b <- 1.0.

x목록 <- (0, 10, 1) 범위.

(x) x목록에 대해: {
  y <- (a=a, b=b, x=x)인 생산 풀기.
  x 보여주기.
  y 보여주기.
}.
