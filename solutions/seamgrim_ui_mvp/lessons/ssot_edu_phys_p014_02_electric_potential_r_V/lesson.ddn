#이름: P014-02 전위 V(r)
#설명: 점전하 주변 전위 V(r)=k/r (교육용 스케일). r=0 회피. (r,V) 출력.

(매마디)마다 {
  k <- 1000.

  r_min <- 1.
  r_max <- 50.
  dr <- 1.

  r목록 <- (r_min, r_max, dr) 범위.

  (r) r목록에 대해: {
    V <- k / r.

    보임 {
      r: r.
      V: V.
    }.
  }.
}.
