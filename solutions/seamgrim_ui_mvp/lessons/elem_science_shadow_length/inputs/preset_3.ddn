// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  x <- 0.
}.

(매마디)마다 {
  a <- 1.
  h <- 6.
  k <- 0.

  x_min <- 0.
  x_max <- 12.
  dx <- 2.

  { x <= x_max }인것 일때 {
    y <- a * (x - h) * (x - h) + k.
    보임 {
      x: x.
      y: y.
    }.
    x <- x + (dx).
  }.
}.
