#이름: 그림자 길이
#설명: 낮 시간에 짧아지는 곡선

(매마디)마다 {
  a <- 1.
  h <- 6.
  k <- 0.

  x_min <- 0.
  x_max <- 14.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- a * (x - h) * (x - h) + k.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
