#이름: elem_econ_saving_goal_variant_저축 목표
#설명: y = 5x

(매마디)마다 {
  k <- 5.
  b <- 0.

  x_min <- 0.
  x_max <- 20.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
