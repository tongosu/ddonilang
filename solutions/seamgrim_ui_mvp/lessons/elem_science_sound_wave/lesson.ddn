
(매마디)마다 {
  A <- 3.
  w <- 1.

  x_min <- 0.
  x_max <- 10.
  dx <- 0.5.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    angle <- w * x.
    s <- (angle) sin.
    y <- A * s.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
