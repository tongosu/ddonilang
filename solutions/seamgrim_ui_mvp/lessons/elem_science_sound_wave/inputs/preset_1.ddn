#이름: elem_science_sound_wave_variant_소리의 진동
#설명: sin 파형

A <- 3.
w <- 1.

x_min <- 0.
x_max <- 10.
dx <- 0.5.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  angle <- w * x.
  s <- (angle) sin.
  y <- A * s.
  x 보여주기.
  y 보여주기.
}.
