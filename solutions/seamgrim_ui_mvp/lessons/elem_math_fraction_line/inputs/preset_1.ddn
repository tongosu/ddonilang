#이름: elem_math_fraction_line_variant_분수와 소수
#설명: y = x/2

(매마디)마다 {
  x_min <- 0.
  x_max <- 10.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- x / 2.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
