#이름: elem_science_plant_growth_variant_식물 성장
#설명: y = 0.5x + 2

k <- 0.5.
b <- 2.

x_min <- 0.
x_max <- 12.
dx <- 1.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  y <- k * x + b.
  x 보여주기.
  y 보여주기.
}.
