#이름: 식물 성장
#설명: y = 0.5x + 2

(매마디)마다 {
  k <- 0.5.
  b <- 2.

  x_min <- 0.
  x_max <- 12.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
