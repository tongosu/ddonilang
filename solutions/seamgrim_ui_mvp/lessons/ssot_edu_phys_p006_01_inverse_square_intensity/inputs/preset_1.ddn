// 출력은 숫자만. (r, I) 순서.

k <- 1000.

r_min <- 1.
r_max <- 20.
dr <- 0.5.

I_of_r = (#ascii1) 수식{ y = k / (x*x) }.

(시작)할때 {
  x <- r_min.
}.

(매마디)마다 {
  { x <= r_max }인것 일때 {
    I <- k / (x*x).
    x <- x + dr.
  }.
}.
