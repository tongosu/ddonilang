#이름: 상점 재고
#설명: y = 50 - x

(매마디)마다 {
  k <- -1.
  b <- 50.

  x_min <- 0.
  x_max <- 32.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x + b.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
