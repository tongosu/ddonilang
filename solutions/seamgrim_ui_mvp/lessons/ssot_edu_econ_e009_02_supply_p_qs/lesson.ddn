#이름: E009-02 공급곡선 qs(p)
#설명: 선형 공급 qs(p)=max(0, c + d*p). (p,qs) 점열 출력.

c <- -20.
d <- 1.5.

p_min <- 0.
p_max <- 80.
dp <- 1.

p목록 <- (p_min, p_max, dp) 범위.

(p) p목록에 대해: {
  qs <- c + (d * p).
  { qs < 0 }인것 일때 {
    qs <- 0.
  }.
  p 보여주기.
  qs 보여주기.
}.
