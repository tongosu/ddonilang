#이름: P011-04 강제진동 공진곡선 A(ω)
#설명: 강제진동의 정상상태 진폭 A(ω)=F0/sqrt((k-mω^2)^2+(cω)^2). (ω,A) 출력.
// 출력은 숫자만. (omega, A) 순서.
// 설정: m=1, k=4(자연각진동수=2), c=0.5, F0=1.

m <- 1.
k <- 4.
c <- 0.5.
F0 <- 1.

w_min <- 0.
w_max <- 4.
dw <- 0.02.

w목록 <- (w_min, w_max, dw) 범위.

(w) w목록에 대해: {
  d <- k - (m*w*w).
  p <- d * d.
  q <- (c*w) * (c*w).
  u <- p + q.
  den <- ((u) sqrt).
  A <- F0 / den.

  w 보여주기.
  A 보여주기.
}.
