#이름: P005-01 이상기체(등온) P-V
#설명: P = n R T / V  (출력: V P)

// 출력은 숫자만. (V, P) 순서.
// NOTE: export_graph.py는 stdout 숫자쌍을 (x,y)로 묶는다.

n <- 1.
R <- 8.314.
T <- 300.

V_min <- 0.5.
V_max <- 5.
dV <- 0.25.

압력식 = (#ascii1) 수식{ y = n*R*T / x }.

V목록 <- (V_min, V_max, dV) 범위.

(V) V목록에 대해: {
  P <- (n=n, R=R, T=T, x=V)인 압력식 풀기.
  V 보여주기.
  P 보여주기.
}.
