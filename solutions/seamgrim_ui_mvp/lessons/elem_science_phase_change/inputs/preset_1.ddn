#이름: elem_science_phase_change_variant_물의 상태 변화
#설명: 간단한 가열 곡선

x_min <- 0.
x_max <- 10.
dx <- 0.5.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  y <- 0.
  고르기:
    { x < 4 }인것: { y <- x. }
    { (x >= 4) 그리고 (x < 7) }인것: { y <- 4. }
    아니면: { y <- 4 + (x - 7) * 0.5. }.
  x 보여주기.
  y 보여주기.
}.
