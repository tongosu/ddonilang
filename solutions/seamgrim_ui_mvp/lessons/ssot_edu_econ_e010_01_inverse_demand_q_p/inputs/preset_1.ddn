#이름: ssot_edu_econ_e010_01_inverse_demand_q_p_variant_E010-01 역수요곡선 P(Q)
#설명: 선형 역수요 P(Q)=max(0, a - b*Q). (Q,P) 점열 출력.

(매마디)마다 {
  a <- 100.
  b <- 1.

  Q_min <- 0.
  Q_max <- 140.
  dQ <- 1.

  Q목록 <- (Q_min, Q_max, dQ) 범위.

  (Q) Q목록에 대해: {
    P <- a - (b * Q).
    { P < 0 }인것 일때 {
      P <- 0.
    }.
    보임 {
      Q: Q.
      P: P.
    }.
  }.
}.
