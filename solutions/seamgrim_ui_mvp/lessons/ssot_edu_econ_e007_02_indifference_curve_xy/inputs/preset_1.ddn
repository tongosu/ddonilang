#이름: ssot_edu_econ_e007_02_indifference_curve_xy_variant_E007-02 무차별곡선 y(x) (U 고정)
#설명: U = sqrt(x*y) (alpha=0.5)에서 U를 고정하면 y = U^2 / x. (x,y) 점열 출력.

(매마디)마다 {
  // 출력은 숫자만. (x, y) 순서.

  U <- 10.
  U2 <- U * U.

  x_min <- 0.5.
  x_max <- 50.
  dx <- 0.5.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- U2 / x.

    보임 {
      x: x.
      y: y.
    }.
  }.
}.
