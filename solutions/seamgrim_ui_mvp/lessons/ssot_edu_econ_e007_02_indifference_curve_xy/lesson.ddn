
(매마디)마다 {
  // 출력은 숫자만. (x, y) 순서.

  U <- 10.
  U2 <- U * U.

  x_min <- 0.5.
  x_max <- 50.
  dx <- 0.5.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- U2 / x.

    보임 {
      x: x.
      y: y.
    }.
  }.
}.
