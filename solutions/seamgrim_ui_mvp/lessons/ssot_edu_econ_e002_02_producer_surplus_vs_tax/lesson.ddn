
(매마디)마다 {
  // 출력은 숫자만. (tau, PS) 순서.

  a <- 100.
  b <- 1.
  c <- 20.
  d <- 1.

  tau_min <- 0.
  tau_max <- 80.
  dtau <- 2.

  PS_of_tau = (#ascii1) 수식{ y = 0.5 * d * ((a - c - x) / (b + d))^2 }.
  tau목록 <- (tau_min, tau_max, dtau) 범위.

  (x) tau목록에 대해: {
    PS <- (x=x, a=a, b=b, c=c, d=d)인 PS_of_tau 풀기.
    보임 {
      x: x.
      PS: PS.
    }.
  }.
}.
