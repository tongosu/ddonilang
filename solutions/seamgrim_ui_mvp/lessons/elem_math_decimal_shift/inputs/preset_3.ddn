#이름: elem_math_decimal_shift_variant_소수점 이동
#설명: y = x * 10

(매마디)마다 {
  x_min <- 0.0.
  x_max <- 9.0.
  dx <- 2.0.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- x * 10.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
