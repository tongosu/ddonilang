# 학생 활동지 — P002-01 일 W-x 그래프

## 목표
- 일(work) W가 거리 x에 비례함을 그래프로 확인한다
- 힘이 일정할 때 W=F*x를 직관적으로 해석한다

## 준비
`lesson.ddn` 상단의 기본값(살림.*)을 아래처럼 바꾸며 실험한다.

기본값(초기):
- `F` = 10
- `x_max` = 10
- `dx` = 0.1

## 해보기
1. F=5,10,20으로 바꿔서 W-x 그래프를 비교해보자.
1. F가 음수(반대방향)일 때 그래프가 어떻게 되는지 생각해보자.

## 질문(생각해보기)
- F를 2배로 하면 그래프는 어떻게 변하는가?
- x_max를 늘리면 어떤 정보가 더 보이는가?
