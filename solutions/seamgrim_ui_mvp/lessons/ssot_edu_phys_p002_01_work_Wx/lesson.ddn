(매마디)마다 {
  // 출력은 숫자만. (x, W) 순서.

  F <- 10.
  dx <- 0.1.
  x_max <- 10.

  x <- 0.

  { x <= x_max }인것 동안: {
    W <- (F=F, x=x) (#ascii) 수식{ F*x }.

    보임 {
      값1: x.
      값2: W.
    }.

    x <- x + dx.
  }.
}.
