#이름: P002-01 일 W-x
#설명: 일: W(x)=F*x (출력: x W)

(매마디)마다 {
  // 출력은 숫자만. (x, W) 순서.

  살림.F <- 10.
  살림.dx <- 0.1.
  살림.x_max <- 10.

  살림.x <- 0.

  { 살림.x <= 살림.x_max }인것 동안: {
    살림.W <- (F=살림.F, x=살림.x) (#ascii) 수식{ F*x }.

    보임 {
      값1: 살림.x.
      값2: 살림.W.
    }.

    살림.x <- 살림.x + 살림.dx.
  }.
}.
