#      균형: a-bw = c+dw -> w*=(a-c)/(b+d), L*=c+d*w*.

(매마디)마다 {
  // 출력은 숫자만. (A, L*) 순서.

  b <- 2.
  c <- 10.
  d <- 1.5.

  A_min <- 60.
  A_max <- 140.
  dA <- 2.

  A목록 <- (A_min, A_max, dA) 범위.

  (A) A목록에 대해: {
    w <- (A - c) / (b + d).
    L <- c + d*w.
    { L < 0 }인것 일때 {
      L <- 0.
    }.
    보임 {
      A: A.
      L: L.
    }.
  }.
}.
