#이름: 구구단 그래프
#설명: y = 3x

(매마디)마다 {
  k <- 3.

  x_min <- 0.
  x_max <- 12.
  dx <- 1.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
