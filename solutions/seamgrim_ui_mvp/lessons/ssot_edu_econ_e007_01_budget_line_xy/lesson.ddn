
(매마디)마다 {
  // 출력은 숫자만. (x, y) 순서.

  I <- 100.
  px <- 10.
  py <- 5.

  x_min <- 0.
  x_max <- I / px.
  dx <- 0.5.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- (I - (px*x)) / py.

    // y < 0 이면 0으로 클램프
    고르기:
      { (y < 0) }인것: { y <- 0. }
      아니면: { y <- y. }
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
