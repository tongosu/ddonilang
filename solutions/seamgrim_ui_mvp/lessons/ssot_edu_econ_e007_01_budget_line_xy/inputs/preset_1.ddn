#이름: ssot_edu_econ_e007_01_budget_line_xy_variant_E007-01 예산선 y(x)
#설명: 예산 I, 가격(px,py)일 때 예산선 y = (I - px*x)/py. (x,y) 점열 출력.
// 출력은 숫자만. (x, y) 순서.

I <- 100.
px <- 10.
py <- 5.

x_min <- 0.
x_max <- I / px.
dx <- 0.5.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  y <- (I - (px*x)) / py.

  // y < 0 이면 0으로 클램프
  고르기:
    { (y < 0) }인것: { y <- 0. }
    아니면: { y <- y. }
  x 보여주기.
  y 보여주기.
}.
