#이름: elem_math_decimal_scale_variant_소수 배율
#설명: y = 0.1x

k <- 0.1.
b <- 0.

x_min <- 0.
x_max <- 100.
dx <- 10.

x목록 <- (x_min, x_max, dx) 범위.

(x) x목록에 대해: {
  y <- k * x + b.
  x 보여주기.
  y 보여주기.
}.
