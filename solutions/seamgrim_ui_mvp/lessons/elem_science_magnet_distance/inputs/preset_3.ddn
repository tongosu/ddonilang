#이름: 자석과 거리
#설명: y = 1/(x+1)

(매마디)마다 {
  x_min <- 0.
  x_max <- 10.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- 1 / (x + 1).
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
