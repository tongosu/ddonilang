// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  x <- 0.
}.

(매마디)마다 {
  x_min <- 0.
  x_max <- 12.
  dx <- 1.

  { x <= x_max }인것 일때 {
    y <- 1 / (x + 1).
    보임 {
      x: x.
      y: y.
    }.
    x <- x + (dx).
  }.
}.
