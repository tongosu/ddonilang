
// stateful-sim preview(auto): range sampler -> step sim

(시작)할때 {
  t <- 0.
}.

(매마디)마다 {
  에너지 = (#ascii1) 수식{ y = a t - b t^2 }.

  a <- 2.4.
  b <- 0.2.

  { t <= 8 }인것 일때 {
    y <- a t - b t^2.
    보임 {
      t: t.
      y: y.
    }.
    t <- t + (1).
  }.
}.
