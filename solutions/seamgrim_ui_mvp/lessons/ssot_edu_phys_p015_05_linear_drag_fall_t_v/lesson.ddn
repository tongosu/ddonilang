
(매마디)마다 {
  g <- 9.8.
  m <- 1.
  b <- 1.

  dt <- 0.02.
  t_max <- 5.

  t <- 0.
  v <- 0.

  t목록 <- (0, t_max, dt) 범위.

  (ti) t목록에 대해: {
    보임 {
      ti: ti.
      v: v.
    }.

    a <- g - ((b / m) * v).
    v <- v + (a * dt).
  }.
}.
