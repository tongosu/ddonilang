
(매마디)마다 {
  a <- 100.
  b <- 2.

  p_min <- 0.
  p_max <- 80.
  dp <- 1.

  p목록 <- (p_min, p_max, dp) 범위.

  (p) p목록에 대해: {
    qd <- a - (b * p).
    { qd < 0 }인것 일때 {
      qd <- 0.
    }.
    보임 {
      p: p.
      qd: qd.
    }.
  }.
}.
