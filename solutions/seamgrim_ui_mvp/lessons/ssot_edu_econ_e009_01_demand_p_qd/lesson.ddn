#이름: E009-01 수요곡선 qd(p)
#설명: 선형 수요 qd(p)=max(0, a - b*p). (p,qd) 점열 출력.

a <- 100.
b <- 2.

p_min <- 0.
p_max <- 80.
dp <- 1.

p목록 <- (p_min, p_max, dp) 범위.

(p) p목록에 대해: {
  qd <- a - (b * p).
  { qd < 0 }인것 일때 {
    qd <- 0.
  }.
  p 보여주기.
  qd 보여주기.
}.
