#이름: high_math_quadratic_variant_high_math_quadratic
#설명: y = a x^2 + b x + c

함수 = (#ascii1) 수식{ y = a x^2 + b x + c }.

a <- 1.
b <- -2.
c <- -3.

x목록 <- (-4, 4, 0.5) 범위.

(x) x목록에 대해: {
  y <- (a=a, b=b, c=c, x=x)인 함수 풀기.
  x 보여주기.
  y 보여주기.
}.
