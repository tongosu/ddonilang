#이름: ssot_edu_s1_function_graph
#설명: 일차함수 y = a*x + b 그래프 스캔
#control: a:수=1.5 [-5..5] step=0.1; b:수=0 [-10..10] step=0.1; x_min:수=-6 [-20..0] step=0.5; x_max:수=6 [0..20] step=0.5; dx:수=0.2 [0.05..2] step=0.05

(매마디)마다 {
  a <- 1.5.
  b <- 0.
  x_min <- -6.
  x_max <- 6.
  dx <- 0.2.

  x <- x_min.
  { x <= x_max }인것 동안: {
    y <- a * x + b.
    보임 {
      x: x.
      y: y.
    }.
    x <- x + dx.
  }.
}.
