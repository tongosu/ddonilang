
(매마디)마다 {
  e_min <- 1.1.
  e_max <- 10.
  de <- 0.1.

  e목록 <- (e_min, e_max, de) 범위.

  (e) e목록에 대해: {
    # 상수탄력성 수요에서 최적 마크업 비율(맛보기): mu = P/MC = e/(e-1).
    mu <- e / (e - 1).

    보임 {
      e: e.
      mu: mu.
    }.
  }.
}.
