
(매마디)마다 {
  // 출력은 숫자만. (Q, P) 순서.
  c <- 0.
  d <- 1.

  Q_min <- 0.
  Q_max <- 100.
  dQ <- 2.

  공급_PQ = (#ascii1) 수식{ y = (x - c) / d }.
  Q목록 <- (Q_min, Q_max, dQ) 범위.

  (x) Q목록에 대해: {
    P <- (x=x, c=c, d=d)인 공급_PQ 풀기.
    보임 {
      x: x.
      P: P.
    }.
  }.
}.
