#이름: 정사각형 둘레
#설명: y = 4x

(매마디)마다 {
  k <- 4.

  x_min <- 0.
  x_max <- 10.
  dx <- 2.

  x목록 <- (x_min, x_max, dx) 범위.

  (x) x목록에 대해: {
    y <- k * x.
    보임 {
      x: x.
      y: y.
    }.
  }.
}.
