#이름: ssot_edu_econ_e008_02_average_cost_q_ac_variant_E008-02 평균비용 AC(q)
#설명: TC(q)=F + c*q + 0.5*d*q^2 에서 AC(q)=TC(q)/q 를 계산해 (q,AC) 출력.

(매마디)마다 {
  // 출력은 숫자만. (q, AC) 순서.
  // q=0에서 나눗셈이 생기므로 q는 0.5부터 시작한다.

  F <- 20.
  c <- 5.
  d <- 0.6.

  q_min <- 0.5.
  q_max <- 50.
  dq <- 0.5.

  q목록 <- (q_min, q_max, dq) 범위.

  (q) q목록에 대해: {
    TC <- F + (c*q) + (0.5*d*q*q).
    AC <- TC / q.

    보임 {
      q: q.
      AC: AC.
    }.
  }.
}.
