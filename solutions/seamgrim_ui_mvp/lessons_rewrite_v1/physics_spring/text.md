# physics_spring

용수철 진동에서 위치(`x`)와 속도(`v`)의 위상 변화를 확인한다.

## 관찰 안내

- 기본 관찰축: `x=t`, `y=x`
- 함께 보기: `v`

## 보개마당 안내

- 도입: 질량(`m`)과 스프링 상수(`k`)를 설정한다.
- 관찰: 시간에 따라 `x`와 `v`가 반복되는 패턴을 확인한다.
- 정리: `k/m` 비율이 커질수록 진동이 빨라지는지 정리한다.

## 실행 구조

- 실행 문법: `채비 {} + (매마디)마다 + 보임 {}`
