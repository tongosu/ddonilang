#이름: seamgrim_ui_mvp_export_parabola
#설명: UI 연동용 포물선 점열 출력(표준 스키마 변환은 외부 툴에서 수행)

포물선 = (#ascii1) 수식{ y = a x^2 + b x + c }.

a <- -1.
b <- 4.
c <- 0.

x목록 <- (0, 4, 1) 범위.

(x) x목록에 대해: {
  y <- (a=a, b=b, c=c, x=x)인 포물선 풀기.
  x 보여주기.
  y 보여주기.
}.
