# std_grid_game_bogae_bridge

Documentation-only skeleton for using `격자게임보기.보개목록` with existing Bogae drawlist resources.

This directory is not loaded by product runtime code and does not define a module/import mechanism.

