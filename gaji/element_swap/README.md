# gaji/element_swap

샘/보개 등 **원소 구현 교체**를 가지로 공급하기 위한 자리입니다.

## 경로
- `overrides/` 아래에 교체 정의 파일을 둡니다.
- 스키마는 임시(placeholder)이며, 실제 적용은 도구에서 **명시적으로 로딩**합니다.

## 목적
- 구현 교체가 필요할 때 **가지 단위로 교체 경로를 고정**합니다.
- 결정성 검증은 기존 `state_hash`/`bogae_hash` 계약을 유지합니다.
