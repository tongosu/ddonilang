# overrides (placeholder)

아래 파일을 두어 교체 경로를 명시합니다.  
현재는 **경로만 예약**하며, 런타임 자동 적용은 하지 않습니다.

## 예상 파일
- `sam.override.detjson`
- `bogae.override.detjson`

## 권장 필드(임시)
- `element`: 대상 원소 ID
- `impl_gaji`: 구현을 제공하는 가지 ID
- `notes`: 이유/근거
