살림.글 <- "a,b,c".
살림.조각 <- (살림.글, ",") 갈라놓기.
살림.합 <- (살림.조각, "-") 이어붙이기.
살림.길 <- (살림.조각) 길이세기.
살림.둘째 <- (살림.조각, 1) 값뽑기.
살림.팩이름 <- (이름="또니", 점수=75).이름.

살림.x <- -9.
살림.abs <- (살림.x) 절댓값.
살림.sqrt <- (살림.abs) 제곱근.
살림.pow <- (살림.sqrt, 2) 제곱.
살림.max <- (3, 5) 최댓값.
살림.min <- (3, 5) 최솟값.

살림.합 보여주기.
살림.길 보여주기.
살림.둘째 보여주기.
살림.팩이름 보여주기.
살림.abs 보여주기.
살림.sqrt 보여주기.
살림.pow 보여주기.
살림.max 보여주기.
살림.min 보여주기.
