# connect_endpoint_reverse_flow_v1

Runner-backed evidence for endpoint inner-sentence `거슬러 흐르게`.

This pack keeps the endpoint paths in sentence order and records only the
direction metadata as `오른쪽에서왼쪽` under the existing
`left_plus_right_zero` convention.

