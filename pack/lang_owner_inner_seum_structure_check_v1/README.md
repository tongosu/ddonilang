# lang_owner_inner_seum_structure_check_v1

Evidence pack for `LANG_OWNER_INNER_SEUM_STRUCTURE_CHECK_V1`.

This pack records a non-executed owner-inner `세움 {}` surface seed:

- owner: `공:임자`
- state group: `성질`
- relation: `세움 { 위치' =:= 속도. }`
- event reaction: `힘가해짐을 받으면 { 속도 <- 속도 + 힘. }`

The pack does not claim parser/runtime support.

The next item is `LANG_CONNECT_LOWERING_TO_SEUM_CHECK_V1`.
