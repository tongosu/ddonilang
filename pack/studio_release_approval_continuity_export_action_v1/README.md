# studio_release_approval_continuity_export_action_v1

This pack seals `STUDIO_RELEASE_APPROVAL_CONTINUITY_EXPORT_ACTION_V1`.

It covers a browser-verified Run screen action that builds a local approval continuity payload from the current registry/share seed export and the required release approval boundary, then copies the payload after a user click.

- schema: `seamgrim.release_approval_continuity_export_action.v1`
- boundary: local continuity copy only, no release approval, no release execution, no GitHub Release, no public upload, no registry publish

