#이름: math_parabola
#설명: 포물선 기본

포물선 <- (#ascii1) 수식{ y = a x^2 + b x + c }.

a <- 1.
b <- 0.
c <- 0.

x목록 <- (-4, 4, 0.5) 범위.

(x) x목록에 대해: {
  y <- (a=a, b=b, c=c, x=x)인 포물선 풀기.
  x 보여주기.
  y 보여주기.
}.
