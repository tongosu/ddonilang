# linear_inequality_solve_minimum_v1

Evidence pack for `LINEAR_INEQUALITY_SOLVE_MINIMUM_V1`.

The pack closes only the minimum one-variable linear inequality helper:

- bounded closed interval
- empty strict interval
- one-sided interval

It does not claim LP/CSP, multi-variable inequalities, endpoint auto-solve, or solver-internal inequality support for `방정식풀기`.

