# pack/bogae_graph_prefix_v1

ROADMAP_V2 `사-1` graph/space2d 첫실행 evidence pack.

이 pack은 `그래프_` / `보개_그래프_` prefix resource를 셈그림 view layer graph로 읽는 계약을 검증한다.
view artifact는 실행 상태 관찰 결과이며 `state_hash`나 replay truth를 소유하지 않는다.

검증:

```powershell
python tests/run_bogae_graph_prefix_check.py
```
