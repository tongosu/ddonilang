# connect_endpoint_carried_property_forward_v1

Runner-backed evidence for endpoint carried-property metadata attached to a
forward carrier flow in the same inner-sentence list.

This pack keeps the flow artifact as `endpoint_flow` and adds an
`endpoint_carried_property` relation item in source order.
