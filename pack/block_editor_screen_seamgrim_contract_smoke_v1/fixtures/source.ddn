채비 {
  값:변수 <- 0.
}.

(시작)할때 {
  { 값 < 2 }인것 동안 {
    값 <- 값 + 1.
  }.

  { 값 == 2 }인것 바탕으로 아니면 {
    "bad" 보여주기.
  } 맞으면 {
    "ok" 보여주기.
  }.

  { 값 >= 0 }인것 다짐하고(알림) 아니면 {
    "warn" 보여주기.
  }.
}.
