# block_editor_screen_seamgrim_contract_smoke_v1

- 목적: `wasm_canon_block_editor_plan()`이 `동안`과 계약(`바탕으로`/`다짐하고`) 블록을
  block editor screen에서 decode/encode 가능한지 고정한다.
- 범위:
  - `while_block`
  - `contract_guard` (`pre abort`, `post alert`)
  - 팔레트 append 1회 (`show`)

