# connect_endpoint_unit_boundary_incompatible_unit_v1

V1M negative evidence that one equality/flow component with incompatible currency families such as `KRW` and `USD` is rejected with `connect_unit_boundary_incompatible_unit`.
