# pack/roadmap_v2_ma2_studio_prereq_unlock_v1

ROADMAP_V2 `마-2` Studio prerequisite unlock evidence.

This pack records that `마-2` can move from `미착수` to `진행` after consuming the available `가-2`, `사-2`, `타-2`, `다-1`, and `사-1` evidence. It is a documentation/checker gate only; it does not close `마-2` and does not add product behavior.

Run:

```powershell
python tests/run_pack_golden.py roadmap_v2_ma2_studio_prereq_unlock_v1
python tests/run_roadmap_v2_ma2_studio_prereq_unlock_check.py
```
