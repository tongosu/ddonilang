f1 <- (#ascii) 수식{x^2 + 1}.
df1 <- (f1) 미분하기.
df1 보여주기.
df1_2 <- (f1, (변수="x", 차수=2)) 미분하기.
df1_2 보여주기.

f2 <- (#ascii) 수식{y = x^2}.
df2 <- (f2) 미분하기.
df2 보여주기.

f3 <- (#ascii) 수식{x^3}.
if3 <- (f3, "x") 적분하기.
if3 보여주기.
if3c <- (f3, (변수="x", 상수포함=참)) 적분하기.
if3c 보여주기.
