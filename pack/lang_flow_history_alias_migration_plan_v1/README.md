# lang_flow_history_alias_migration_plan_v1

Evidence pack for `LANG_FLOW_HISTORY_ALIAS_MIGRATION_PLAN_V1`.

This pack closes the migration plan for moving the value-stream/ring-buffer family toward `이력` while preserving connector/port `흐름` and current runtime compatibility.

It makes no runtime, stdlib, parser/frontdoor, SSOT, lesson schema, active allowlist, or UI landed claim.
