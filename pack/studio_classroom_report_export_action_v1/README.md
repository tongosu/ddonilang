# studio_classroom_report_export_action_v1

This pack seals `STUDIO_CLASSROOM_REPORT_EXPORT_ACTION_V1`.

It records the Run screen classroom report export behavior:

- schema: `seamgrim.classroom_report_export_action.v1`
- UI location: Mirror tab, save/export tools
- behavior: user copies a local classroom TSV report for the current Run lesson/mode
- evidence: preview text, clipboard text, and browser instrumentation update

The pack does not claim accounts, cloud sync, permission systems, remote save, parser/frontdoor changes, runtime changes, lesson schema changes, public upload, or `docs/ssot/**` changes.
