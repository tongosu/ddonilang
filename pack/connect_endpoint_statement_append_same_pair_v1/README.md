# connect_endpoint_statement_append_same_pair_v1

Runner-backed evidence for consecutive same-target endpoint connect statements
over the same endpoint pair.

The resulting artifact is `endpoint_statement_set`; each `이음들` item keeps
the existing single-statement artifact shape.
