# 테스트

## 실행
- `cargo run -p teul-cli -- run pack/gogae4_w40_sitcom_engine_v1/input.ddn --madi 5 --seed 0x0 --geoul-out build/geoul/w40`
- `cargo run -p teul-cli -- story make --geoul build/geoul/w40 --out build/geoul/w40/story/story.detjson`

## 검증
- story.detjson 내용 확인
