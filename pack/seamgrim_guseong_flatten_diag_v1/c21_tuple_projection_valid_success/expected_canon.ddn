짜임 {
  형식: 진자_틀.
  입력 {
    기준점: (수,수) <- (0.0, 0.0).
  }.
  출력 {
    끝점: (수,수) <- (0.0, 0.0).
  }.
}.
a <- (L=1.0)인 진자_틀.
b <- (L=0.8)인 진자_틀.
b <- (b, "기준점", ((b, "기준점") 짝맞춤.필수값, "0", a.끝점.1) 짝맞춤.바꾼값) 짝맞춤.바꾼값.
