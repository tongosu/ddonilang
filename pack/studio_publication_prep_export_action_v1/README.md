# studio_publication_prep_export_action_v1

This pack seals `STUDIO_PUBLICATION_PREP_EXPORT_ACTION_V1`.

It covers a browser-verified Run screen action that builds a local publication-prep payload from the current lesson package and the existing lesson publication review surface, then copies the payload after a user click.

- schema: `seamgrim.publication_prep_export_action.v1`
- boundary: local-only prep copy, no public upload, no registry publish, no account, no cloud sync, no remote save

