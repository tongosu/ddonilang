# connect_endpoint_statement_append_mixed_pair_v1

Runner-backed evidence for consecutive same-target endpoint connect statements
over different endpoint pairs.

The outer `endpoint_statement_set` does not carry a shared endpoint pair; each
item keeps its own endpoint artifact shape.
