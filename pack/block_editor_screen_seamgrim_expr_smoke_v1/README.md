# block_editor_screen_seamgrim_expr_smoke_v1

- 목적: `BlockEditorScreen`이 일반 call statement를 `expr_stmt` block으로 decode/encode하는지 고정한다.
- 범위:
  - `(시작)할때` 안의 일반 식 실행문
  - `() 증명.`
  - `(세움=거리_0이상, 값들=(거리=3)) 살피기.`
  - 팔레트 append 1회 (`expr_stmt`)
