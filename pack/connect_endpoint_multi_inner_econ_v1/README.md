# connect_endpoint_multi_inner_econ_v1

Runner-backed economic-style multi inner-sentence evidence.

This pack combines endpoint equality, forward flow, and reverse flow in one
`잇기(...)` surface and verifies that `관계들` keeps the source order.

