# studio_education_operations_lts_export_action_v1

This pack records the browser-verified export action for `STUDIO_EDUCATION_OPERATIONS_LTS_EXPORT_ACTION_V1`.

The behavior is local-only: the Run screen exposes an Education operations LTS preview and a user-clicked copy action. The copied payload bridges classroom report, local package, publication prep, registry seed, release approval continuity, and benchmark/LTS matrix evidence into one operations packet.

This pack does not claim LTS certification, benchmark execution, performance baseline publication, release approval, release execution, GitHub Release creation, public upload, registry publication, public link creation, package install enablement, publication snapshot emission, archive generation, checksum generation, cloud sync, account setup, permission system changes, remote save, active allowlist mutation, parser/frontdoor grammar changes, runtime changes, or SSOT edits.
