# studio_classroom_mode_switch_v1

This pack seals `STUDIO_CLASSROOM_MODE_SWITCH_V1`.

It records the Run screen classroom mode switch behavior:

- schema: `seamgrim.classroom_mode_switch.v1`
- UI location: Run control bar beside the run preset rail
- behavior: user clicks `학생` or `교사` and the existing onboarding profile changes immediately
- evidence: run preset rail, active button state, onboarding status, and browser instrumentation update

The pack does not claim accounts, cloud sync, permission systems, remote save, parser/frontdoor changes, runtime changes, lesson schema changes, public upload, or `docs/ssot/**` changes.
