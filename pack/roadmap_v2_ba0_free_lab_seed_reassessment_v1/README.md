# roadmap_v2_ba0_free_lab_seed_reassessment_v1

ROADMAP_V2 `바-0` 자유 실험실 seed 재평가 pack이다.

이 pack은 새 자유 실험실 제품 동작을 추가하지 않는다. 기존 seed pack과 `바-1`~`바-5` 제품 경로 검증을 묶어 `바-0` 빈 작업실 설계가 현재 행렬에서 `닫힘-동작`으로 재분류될 수 있음을 고정한다.

## Verification

```powershell
python tests/run_pack_golden.py roadmap_v2_ba0_free_lab_seed_reassessment_v1
python tests/run_roadmap_v2_ba0_free_lab_seed_reassessment_check.py
```
