# std_block_piece_geometry_v1

`STD_BLOCK_PIECE_MINIMUM_V1` geometry evidence.

This pack fixes the product builtin path for `블록조각.만들기`,
`블록조각.칸목록`, `블록조각.이동`, and `블록조각.회전`.
It does not add parser/frontdoor syntax.
