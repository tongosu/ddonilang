# connect_endpoint_unit_boundary_dim_conflict_v1

V1M negative evidence that one equality/flow component with incompatible dimensions such as `m` and `N` is rejected with `connect_unit_boundary_dim_conflict`.
