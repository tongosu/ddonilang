# lesson_econ_01 — 수요·공급 균형(한국어 Manim MVP)

포함:
- lesson.ddn: #control(income) + 보개장면(progress) + graph(xy_line) + table(행) + text + structure(인과지도)
- meta.toml
- expected.scene.v0.json / expected.session.v0.json
- expected.graph.v0.json (baseline income=0) + generate_expected_graph.py

주의:
- table은 `행` 채널(값꾸러미)을 반복 출력합니다(도구가 누적/표시).
- graph 오버레이는 graph_kind + 축 메타 동일 조건에서만 허용(SSOT).
