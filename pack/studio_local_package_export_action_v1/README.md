# studio_local_package_export_action_v1

This pack seals `STUDIO_LOCAL_PACKAGE_EXPORT_ACTION_V1`.

It covers a browser-verified Run screen action that builds a local Studio package payload, renders its index preview, and copies the payload through a user click.

- schema: `seamgrim.local_package_export_action.v1`
- boundary: local-only copy, no account, no cloud sync, no public registry, no remote save

