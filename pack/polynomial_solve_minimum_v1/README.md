# polynomial_solve_minimum_v1

Evidence for `POLYNOMIAL_SOLVE_MINIMUM_V1`.

The pack verifies that `다항식.풀기(수식값, 변수)` is a product helper over the existing exact relation solver. It fixes a single-root quadratic representative and preserves existing `non_unique` and `unsupported` boundaries.

