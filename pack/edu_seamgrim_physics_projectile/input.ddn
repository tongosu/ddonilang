#이름: physics_projectile
#설명: 2D 투사체 궤적(단순)

수평 <- (#ascii1) 수식{ x = vx t }.
수직 <- (#ascii1) 수식{ y = vy t - 0.5 g t^2 }.

vx <- 4.
vy <- 8.
g <- 9.8.

t목록 <- (0, 1.6, 0.2) 범위.

(t) t목록에 대해: {
  x <- (vx=vx, t=t)인 수평 풀기.
  y <- (vy=vy, g=g, t=t)인 수직 풀기.
  x 보여주기.
  y 보여주기.
}.
