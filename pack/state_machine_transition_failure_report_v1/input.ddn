매틱:움직씨 = {
    전이_실패 <- 세움{
        { 현재상태 == 다음상태 }인것 바탕으로(물림) 아니면 {
            없음.
        }.
    }.
    기계 <- 상태머신{
        빨강, 초록 으로 이뤄짐.
        빨강 으로 시작.
        빨강 에서 초록 으로.
        전이마다 전이_실패 살피기.
    }.
    현재 <- (기계) 처음으로.
    다음 <- (기계, 현재) 다음으로.
}

