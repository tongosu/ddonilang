# block_editor_screen_seamgrim_smoke_v1

- 목적: `BlockEditorScreen`이 일반 셈그림 DDN을 `wasm_canon_block_editor_plan()`으로 decode하고,
  `텍스트로`/`실행` 콜백까지 같은 생성 DDN을 넘기는지 고정한다.
- 범위:
  - `채비` 블록 + `매김(간격)` + 일반 값/변수 항목
  - `(시작)할때`, `(매마디)마다`
  - `보여주기`, 대입, `일때/아니면`
  - 팔레트 append 1회 (`show`)

