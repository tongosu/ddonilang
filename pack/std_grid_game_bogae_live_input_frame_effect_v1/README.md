# std_grid_game_bogae_live_input_frame_effect_v1

Input-vs-control frame effect evidence for finite std grid-game Bogae live output.

The checker runs a no-input control first, then an input run driven by real Chromium keyboard events, and compares final manifest frame metadata after the first sampled input plus three ticks.
