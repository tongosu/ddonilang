# studio_public_lesson_publication_prep_v1

This pack records the local evidence for `STUDIO_PUBLIC_LESSON_PUBLICATION_PREP_V1`.

It prepares representative lesson publication candidates without uploading files, publishing to a registry, creating a GitHub Release, generating archives, generating publication checksums, syncing cloud state, adding accounts, changing permissions, or changing product/runtime behavior.

Verification:

```powershell
python tests/run_pack_golden.py studio_public_lesson_publication_prep_v1
python tests/run_studio_public_lesson_publication_prep_check.py
```
