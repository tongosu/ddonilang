채비 {
  값:변수 <- 1.
}.

(시작)할때 {
  ?? {
    "prompt" 보여주기.
  }

  "answer" ??: {
    "after" 보여주기.
  }

  { 값 > 0 }인것 ??
  ?? {
    "cond" 보여주기.
  }

  ??: 
    { 값 < 2 }인것: {
      "low" 보여주기.
    }
    ??: {
      "else" 보여주기.
    }
}.
