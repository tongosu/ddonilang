# D-PACK: input_key_alias_ko_v1

## 목적
- 키보드 한국어 별칭 입력이 정본 키와 동일하게 동작함을 검증한다.
- 정본 키와 별칭 키가 같은 샘(입력) 결과를 만들어야 한다.

## 구성
- `input.ddn`: 정본/별칭 키 상태를 비교 출력
- `sam/key_alias.input.bin`: 1틱 입력 테이프(오른쪽 키 눌림)
- `tests/README.md`: 수동 실행 가이드

## DoD(최소)
- 정본 키/별칭 키의 `눌림`/`누르고있음` 결과가 동일하다.
