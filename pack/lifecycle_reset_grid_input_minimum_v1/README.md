# lifecycle_reset_grid_input_minimum_v1

lifecycle/reset/grid/input minimum floor를 runner-backed evidence로 묶는다.

- `누리다시` / `모두다시`
- `시작하기` / `넘어가기`
- state/view boundary smoke 재사용
- grid2d smoke는 backing runner로 연결만 유지

