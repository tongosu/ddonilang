# seamgrim_malblock_0_v1

ROADMAP_V2 `라-0` 말블록 설계 계약 evidence pack.

이 pack은 말블록이 DDN 정본을 대체하지 않고, 셈그림 편집 표현으로만 동작해야 한다는 경계를 검증한다.

검증 명령:

```sh
python tests/run_seamgrim_malblock_design_check.py
```
