# pack/vol4_event_dispatch_runtime_v1

- 목적: 또니랑 4권 앞부분의 `알림씨 / 받으면 / 임자 / dispatch` bounded runtime 흐름을 current runtime에서 고정한다.
- 범위:
  - single/dual event dispatch
  - actor-targeted handling
  - deterministic last-write/handling count

