# lang_history_alias_stdlib_bridge_v1

This pack seals `LANG_HISTORY_ALIAS_STDLIB_BRIDGE_V1`.

It records product stdlib alias support for `이력.*` while preserving `흐름.*`, legacy aliases, `ddn.stream.v1`, and stream sidecar compatibility.
