# connect_endpoint_multi_inner_v1

Runner-backed evidence for supported comma-separated endpoint inner sentences.

This pack keeps single-rule artifact shapes unchanged and records the multi-rule
surface as an `endpoint_relation_set` whose `관계들` list preserves source order.

