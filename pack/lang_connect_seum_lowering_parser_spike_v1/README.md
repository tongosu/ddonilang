# lang_connect_seum_lowering_parser_spike_v1

Evidence pack for `LANG_CONNECT_SEUM_LOWERING_PARSER_SPIKE_V1`.

This pack keeps the executable runtime surface on the existing endpoint relation pack path and pairs it with Rust tests for `ddonirang_lang::connect_endpoint_relation_seum_rows`.

The pack does not claim a new `잇기 {}` block or a `세움 {}` solver.
