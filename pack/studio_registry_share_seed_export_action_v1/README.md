# studio_registry_share_seed_export_action_v1

This pack seals `STUDIO_REGISTRY_SHARE_SEED_EXPORT_ACTION_V1`.

It covers a browser-verified Run screen action that builds draft-only registry/share seed rows from the current publication prep candidate set, then copies the seed payload after a user click.

- schema: `seamgrim.registry_share_seed_export_action.v1`
- boundary: draft-only local copy, no registry publish, no public upload, no public link creation, no install enablement, no cloud sync

