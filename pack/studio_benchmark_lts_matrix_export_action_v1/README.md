# studio_benchmark_lts_matrix_export_action_v1

This pack seals `STUDIO_BENCHMARK_LTS_MATRIX_EXPORT_ACTION_V1`.

It covers a browser-verified Run screen action that builds a local benchmark/LTS readiness matrix from the current approval continuity payload, then copies the matrix payload after a user click.

- schema: `seamgrim.benchmark_lts_matrix_export_action.v1`
- boundary: local matrix copy only, no benchmark execution, no performance baseline, no LTS certification, no release execution

