# seamgrim_malblock_roundtrip_subset_v1

Bounded ROADMAP_V2 `라-2` evidence for DDN -> 말블록/block plan -> DDN roundtrip.

The checker reuses `tests/block_editor_roundtrip_runner.mjs` for each fixture case, then validates canonical equality, raw-block boundaries, required block kinds, and selected encoded-runtime stdout.

This pack does not claim full block editor UI integration or full grammar support.
