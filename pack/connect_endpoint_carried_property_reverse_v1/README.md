# connect_endpoint_carried_property_reverse_v1

Runner-backed evidence for endpoint carried-property metadata attached to a
reverse carrier flow in the same inner-sentence list.

This pack keeps the reverse-flow artifact as `endpoint_flow` and records the
carried property with `운반방향 = "오른쪽에서왼쪽"`.
