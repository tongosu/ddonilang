# std_grid_game_bogae_live_input_contract_v1

Runner-backed contract marker for the std grid-game Bogae live web input bridge.

The dynamic `/input` endpoint check uses a free port in `tests/run_std_grid_game_bogae_live_bridge_check.py`.
