#이름: newton_motion_josa
#설명: 뉴턴 운동 법칙 — 조사 바인딩 셈그림 데모

// 씨앗 정의: 조사로 역할 구분
(힘~을, 질량~으로) 가속도구하기: 셈씨 = {
  결과 <- 힘 / 질량.
  결과 되돌림.
}.

// 초기 조건
F <- 10.
m <- 2.
dt <- 0.1.
v <- 0.
x <- 0.

// 가속도 = F/m (조사 바인딩: 순서 바꿔도 올바르게 매핑)
a <- (m~으로, F~을) 가속도구하기.

// 시뮬레이션
t목록 <- (0, 20, 1) 범위.

(tick) t목록에 대해: {
  t <- tick * dt.
  v <- v + a * dt.
  x <- x + v * dt.

  t 보여주기.
  x 보여주기.
}.
