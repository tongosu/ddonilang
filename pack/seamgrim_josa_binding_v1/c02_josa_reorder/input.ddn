#이름: josa_reorder
#설명: 조사 바인딩 — 인자 순서 바꾸기 검증

// 빼기: 대상에서 값을 뺀다
(대상~을, 값~으로) 빼기: 셈씨 = {
  결과 <- 대상 - 값.
  결과 되돌림.
}.

// 정순: (10~을, 3~으로) → 대상=10, 값=3 → 10-3=7
a <- (10~을, 3~으로) 빼기.
a 보여주기.

// 역순: (3~으로, 10~을) → 대상=10, 값=3 → 10-3=7 (조사로 올바르게 매핑)
b <- (3~으로, 10~을) 빼기.
b 보여주기.

// 위치 기반 (조사 없음): (10, 3) → 대상=10, 값=3 → 10-3=7
c <- (10, 3) 빼기.
c 보여주기.
