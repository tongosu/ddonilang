# ode_method_comparison_v1

This pack compares the existing `적분.오일러` and `적분.반암시적오일러` helpers on one deterministic finite tick sample.

It proves method-comparison lesson output only. It does not claim a new ODE solver, RK4, or adaptive stepping.

