# Intent

Pin Seulgi v1 as a boundary contract:

1. role is advisor, not direct executor
2. gatekeeper approval is required before commit
3. target madi is fixed at injection time
4. trace/debug fields do not perturb state hash
5. replay uses recorded injections only
