# 가나다 15줄기×6마루 — 실증 기반 정정 매트릭스 (2026-07-06)

> 원본(`ROADMAP_V2_GANADA_MILESTONE_MATRIX_20260426.md`)의 90칸 "닫힘-동작" 주장을 Q23~Q25(2026-07-06)로 전수 재검증한 결과. 원본은 폐기하지 않되, **상태 칸은 이 문서를 정본으로 삼는다.**

## 총계

| 재판정 | 칸 수 | 비율 |
|---|---:|---:|
| 진짜닫힘 | 8 | 9% |
| 존재+PASS이나형식뿐 | 8 | 9% |
| 존재하나FAIL | 74 | 82% |
| **원본 주장(참고)** | **90 (100%)** | — |

## 핵심 발견 — 검증 체계 자체의 결함

FAIL 74칸의 압도적 다수는 "기능이 안 돌아가서"가 아니라 **"완료를 증명하는 상태 문서(.md)가 없어서"**다. 즉 이 로드맵의 "체커"는 대부분 **기능이 아니라 문서 존재 여부**를 검사하도록 지어졌다 — 지난 몇 주 동안 "닫힘"이 실제 구현이 아니라 상태 마크다운 파일 작성으로 수렴해온 정황과 정확히 일치한다.

## 실제로 닫힌 것 (8칸 — 유일한 신뢰 가능 구간)

- **바줄기(자유 실험실) 전체(바-1~바-5)**: UI 빌더·golden·node runner 전부 실제 동작, placeholder 없음.
- **자줄기(AI·슬기) 절반(자-1, 자-2, 자-4)**: SeulgiIntent/Gatekeeper/model artifact **백엔드**는 실제로 검증됨. 단 자-3/자-5(제안 UI, replay-safe workflow)는 HTML에 진입점 selector 자체가 없어서 실패 — 백엔드는 있는데 화면 배선이 안 됨.

## 형식만 있는 것 (8칸)

가-4/나-4(로컬 레지스트리, Q21과 일치 — 부분 착지), 하-0~하-3(교육 UI, 로컬 evidence만·live/원격 기능 전부 false claim), 자-0, 파-0(재평가 marker, 실제 기능 확장 아님).

## 가장 중요한 실용적 발견 — 마줄기(교과 lesson)

placeholder(`edu_p1_constant_accel` 등, `보개로 그려.` 한 줄)만 있는 게 아니라 **이미 실질적인 대표 lesson이 존재**한다:
- `edu_seamgrim_rep_phys_projectile_xy_v1`, `edu_seamgrim_rep_econ_supply_demand_tax_v1` — 채비/시작/매마디를 갖춘 진짜 모델, 실행도 됨. **`golden.jsonl`만 없어서 검증 파이프라인에 안 걸림.**
- `physics_pendulum_seed_v1`, `econ_tax_shock_supply_demand_seed_v1` — 진짜 모델이나 **`E_LEGACY_RANGE_SYNTAX`**(구형 `//범위(...)` 주석 — 메모리의 OI-MAEGIM-MIGRATION 이슈와 동일)로 실행 자체가 실패.

**이건 "새로 지어야 할 일"이 아니라 "이미 지어진 걸 golden 추가 + 구문 이전만 하면 되는 일"이다.** D37(대표 데모 1호=포물선)과 직결.

## 줄기별 요약

| 줄기 | 진짜닫힘 | 형식뿐 | FAIL | 비고 |
|---|---:|---:|---:|---|
| 가(언어코어) | 0 | 0 | 6 | Q18/Q20과 일치. 커널 구현 게이트로 이미 추적 중 |
| 나(표준가지) | 0 | 1(나-4) | 5 | 가-4와 동일 로컬 레지스트리 이슈 |
| 다(수학) | 0 | 0 | 6 | 전부 frontier/rebase marker, 실기능 검증 안 됨 |
| 라(말블록) | 0 | 0 | 6 | 우선순위 1위였으나 전부 FAIL |
| 마(셈그림교과) | 0 | 0 | 6 | placeholder+구형lesson, 그러나 수리 가능한 실질 자산 존재 |
| 바(자유실험실) | **5** | 0 | 1(바-0) | **유일한 완전 닫힘 트랙** |
| 사(보개/게임) | 0 | 0 | 6 | 사-2 미닫힘이 사-3/4를 연쇄 차단 |
| 아(누리짐) | 0 | 0 | 6 | 메모리 기록(아-2 stale)과 일치 확인 |
| 자(AI/슬기) | **2**(자1,2) +1(자4) | 1(자-0) | 2 | 백엔드 실질, UI 배선만 미비 |
| 차(RPG) | 0 | 0 | 6 | 전부 "닫힘 문서 없음" |
| 카(또니마루) | 0 | 0 | 6 | 전부 "닫힘 문서 없음" |
| 파(경제사회) | 0 | 1(파-0) | 5 | 사줄기 의존으로 연쇄 차단 |
| 거(AI개발보조) | 0 | 0 | 6 | 전부 "닫힘 문서 없음" |

## 재우선순위 제안 (Claude, 사용자 확정 필요)

1. **마줄기 lesson 수리** — 즉시 착수 가능, 작고 명확. D37 대표 데모와 직결. (golden.jsonl 2건 추가 + 구형 range 구문 2건 매김{} 이전)
2. **자줄기 UI 배선** — 백엔드 이미 실질적이므로 HTML selector만 추가하면 자-3/5도 진짜닫힘 가능.
3. **가줄기(언어코어)** — 이미 별도 트랙(Q20 진단 결과 반영, 커널 구현 게이트)으로 추적 중. 이 매트릭스 재검증은 그 필요성을 다시 한번 확인해준 것으로 충분, 중복 착수 불필요.
4. **나머지 8줄기(다/라/사/차/카/파/거/아)** — "닫힘 문서 없음"류 FAIL은 진짜 기능 상태를 아직 모른다는 뜻(체커가 문서만 봄). 지금 급하게 손댈 필요는 없고, H1(마줄기 우선) 이후 순차 재검토.
