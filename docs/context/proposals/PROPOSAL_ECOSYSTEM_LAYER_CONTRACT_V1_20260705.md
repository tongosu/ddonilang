# PROPOSAL_ECOSYSTEM_LAYER_CONTRACT_V1 (2026-07-05, 초안)

> 작성: Claude / 상태: 초안 — 사용자 확정 대기
> 목적: 결정론 트랙(이야기/누리)과 비결정론 트랙(보개/샘 확장, 즉 가지·렌더러 직접 저작)의 경계를 명문화한다.
> 관계: `PROPOSAL_LANG_KERNEL_V1_DIRECTION_20260704.md`(D1~D37)의 자매 문서. 커널이 "언어 안"을 다뤘다면 이 문서는 "언어 밖 — 언어를 감싸는 계층 확장"을 다룬다.
> 실행 범위: 이 문서는 계약(문서)만 정리한다. SDK 구현은 하지 않는다 — AGE 게이트에 따라 별도 착수.

## 배경

2026-07-06 자줄기 UI 배선(Q26)에서 Codex가 "새 마운트 방식을 발명하지 마라"는 지시를 받고 기존 UI 패널 구조를 그대로 베껴 붙였다. 작동은 했지만 이것은 **계약이 아니라 관례**다. 셈그림/AI Gym/게임엔진이 각자 보개 확장(렌더러)을 늘려갈수록, 매번 "무엇을 베낄까"부터 다시 시작하는 방식은 어긋나기 시작한다.

동시에 사용자 요청(2026-07-05): "결정론 기반(시뮬레이션/교육/AI이용/AI검증)과 결정론 없는 버전(가지/보개 직접 설계)을 6계층 위에서 다 고려된 상태로 정리하고 싶다." 이 문서는 그 정리다.

## 원 논의 D39~D43 → 이 제안의 3항목 통합

최초 논의에서는 D39(성역 공식화)/D40(보개 SDK)/D41(샘 확장)/D42(슬기 경계 재확인)/D43(거울 소비 도구 비결정론 허용)의 5항목으로 스케치했다. 재검토 결과 D42는 D41의 특수사례, D43은 D40과 본질적으로 같은 범주(read-only observer)임을 확인해 **3항목으로 통합 권장**한다.

| 원 항목 | 통합 결과 | 사유 |
|---|---|---|
| D39 | 그대로 유지 | 유일하게 완전히 새로운 결정 |
| D40 | D40에 통합(핵심) | 보개 SDK 자체 |
| D41(신) | 별도 유지 | 샘 확장 자체 |
| D42(구, 슬기 경계) | D41(신)에 흡수 | "슬기는 입력원천의 하나, 특권 경로 없음" = 샘 확장 계약의 인스턴스 |
| D43(구, 거울 소비) | D40(신)에 흡수 | "거울 로그를 읽기만 하는 도구" = 보개와 동일한 read-only observer 카테고리 |

---

## D39 — 결정론 성역 공식화 (이야기/누리)

**설명:** 현재 DR-152/DR-178은 "state_hash에 무엇이 포함되는가"를 규정하지만, "이야기/누리 계층에서 어떤 코드가 실행될 수 있는가"는 규정하지 않는다. 실질적으로는 이미 언어(DDN)로 작성된 코드만 이 계층에서 도는데, 이걸 명문화해두지 않으면 나중에 "성능을 위해 네이티브 확장 포인트를 이야기 계층에 추가하자"는 제안이 나왔을 때 막을 SSOT 근거가 없다.

**신규 제안 문구(안):**
> 이야기(Iyagi)와 누리(Nuri) 계층에서 실행되는 모든 로직은 오직 또니랑(DDN) 언어로 작성된 코드여야 한다. 네이티브 플러그인, 동적 라이브러리 로드, 임의 스크립팅 훅은 이 두 계층에 들어올 수 없다. 성능 최적화는 언어 처리기(파서/정본화기/런타임) 내부 구현에서만 이뤄지며, 사용자/AI가 저작하는 지점(가지, lesson)은 항상 DDN 표면 문법을 통과한다.

**Claude 추천:** 채택. 비용이 거의 없고(현재 동작을 문서화할 뿐), 나중에 압박이 왔을 때 확실한 방어선이 된다.

**결정 필요:** 이 문구를 SSOT_MASTER의 어느 절에 반영할지(신규 절 vs 기존 아키텍처 절 보강)는 사용자가 SSOT 반영 시 정할 사항. 지금은 승인/보류만 결정하면 됨.

---

## D40 — 관찰자 계약 (보개 SDK + 거울 소비 도구 통합)

**설명:** 보개(Bogae)는 이미 "read-only 관찰/렌더"로 정의되어 있고, AGE2 로드맵에 `ObservationManifest`(엔진 네이티브 생성, 현재는 UI 합성이 fallback — `SSOT_MASTER_v24.12.9.md:1253,1283`)가 계획되어 있다. 즉 "무엇을 관찰할 수 있는가"를 엔진이 선언하는 개념 자체는 이미 SSOT에 있다. 지금 없는 건 "그 관찰 대상을 소비해서 임의의(비결정론적) 렌더러/분석 도구를 만드는 쪽"의 계약이다.

거울(Geoul)이 기록한 로그(사후 분석 도구)도 성격이 같다 — 이미 결정론적으로 박제된 사실을 읽기만 하므로, 소비하는 도구 쪽이 아무리 비결정론적이어도(랜덤 색상, AI 요약, 애니메이션 타이밍) 원본에 영향이 없다.

**신규 제안 문구(안):**
> **관찰자 계약(Observer Contract):** 보개 확장(렌더러/UI 패널)과 거울 로그 소비 도구는 다음을 만족하면 임의로(비결정론적으로) 저작될 수 있다.
> 1. `ObservationManifest`(또는 거울 기록 파일)로 명시적으로 선언된 대상만 읽는다 — 임의 내부 상태 직접 접근 금지.
> 2. 누리(Nuri) world에 쓰기 접근을 하지 않는다(엔진 mutation 함수 호출 금지).
> 3. 확장의 내부 로직(렌더링 타이밍, 색상 난수, 3rd-party 라이브러리 사용 등)은 `state_hash`/`trace_hash`에 어떤 영향도 주지 않는다.
> 이 3원칙을 만족하는 한, 보개 확장은 결정론 요구사항 밖에 있다 — 이것이 "가지/보개 직접 설계(결정론 없는 버전)" 트랙의 공식 근거다.

**Claude 추천:** 채택, 우선순위 최상위. Q26이 실제로 겪은 문제(마운트 관례 베끼기)를 해결하는 가장 직접적인 항목. 단, SDK 코드 자체는 지금 만들지 않는다 — 문서화만 지금 하고, 실제 구현은 AGE2 `ObservationManifest` 엔진 네이티브 경로가 고정된 뒤 진행(이미 SSOT 로드맵에 순서가 있음).

**결정 필요:** 이 3원칙 문구가 맞는지, 특히 "거울 소비 도구"를 보개와 같은 계약으로 묶는 것에 동의하는지.

---

## D41 — 확장 입력 계약 (샘 + 슬기 통합)

**설명:** 이미 SSOT_MASTER `외부개입 공통 경계 — MUST`(L835-849)에 원칙이 있다: 외부개입은 세계 상태를 직접 수정하지 않고, 반드시 샘으로 정규화되며, 거울에 기록 가능해야 한다. 입력원천 6값(`사람/슬기/밖일/일정/이어전달/펼침실행`)도 이미 잠겨 있다. 즉 **D41은 새 규칙이 아니라 기존 MUST-절을 "미래의 신규 입력원천에도 동일하게 적용된다"고 명시적으로 확인하는 것**이다 — 예를 들어 게임 컨트롤러, 네트워크 멀티플레이어 입력, 외부 센서 데이터 같은 것을 나중에 추가하더라도 이 6값 스키마의 확장으로 다뤄야지, 별도 특권 경로를 만들면 안 된다.

슬기(Seulgi)는 이미 이 6값 중 하나(`슬기`)로 들어와 있으므로, "슬기가 제안한 행동이 실제로 반영되려면 항상 샘을 거친다"는 건 새 결정이 아니라 이 계약의 인스턴스임을 재확인하는 것뿐이다.

**신규 제안 문구(안):**
> 향후 추가되는 모든 신규 입력원천(게임 컨트롤러, 네트워크, 센서 등)은 기존 `입력원천` 6값 스키마의 확장으로 편입되어야 하며, `외부개입 공통 경계 — MUST`(직접 상태 변경 금지/샘 경유/거울 기록 가능)를 동일하게 따른다. 슬기(AI 어드바이저)를 포함한 어떤 입력원천도 이 경계를 우회하는 별도 경로를 가질 수 없다.

**Claude 추천:** 채택하되 "재확인" 성격임을 명확히. 실질적으로 이미 SSOT에 있는 내용이라 리스크가 거의 없다.

**결정 필요:** 이 재확인 문구를 신규 D-번호로 남길지, 아니면 그냥 기존 MUST-절 옆에 각주로 붙일지(별도 결정번호가 필요 없을 수도 있음).

---

## Codex 검증 위임 (실행 전 필수)

이 문서의 D40/D41이 "이미 대체로 사실"이라는 주장은 검증이 필요하다 — SSOT 문면과 실제 코드가 항상 일치하는 건 아니었다(이번 세션에서 반복 확인됨). 실제 코드에서 다음을 확인하기 전에는 이 문서를 확정하지 않는다:

1. 현재 UI 보개 확장(예: `seulgi_proposal_ui.js`, `seulgi_replay_safe_workflow.js`, 기타 render 모듈)이 실제로 Nuri mutation 함수를 호출하는 경로가 있는지(D40 위반 여부).
2. `tools/teul-cli/src/runtime`에서 `InputSnapshot`/`ai_injections`/`SeulgiIntent` 등 입력 경로가 실제로 전부 샘 경계(`begin_tick`)를 통과하는지, 우회 경로가 있는지(D41 위반 여부).
3. 이야기/누리 계층 실행 경로에 DDN 언어가 아닌 네이티브 훅/플러그인 로드 지점이 실제로 있는지(D39 위반 여부).

→ 별도 브리프(`BRIEF_ECOSYSTEM_CONTRACT_VERIFICATION_V1.md`)로 Codex에 위임함. 위반이 발견되면 이 문서의 해당 항목은 "이미 사실"이 아니라 "고쳐야 할 로드맵 항목"으로 재분류한다.

## 다음 단계

1. Codex 검증(Q28) 결과 회수 및 리뷰.
2. 검증 결과에 따라 D39/D40/D41 문구 최종 확정(위반 없으면 그대로, 있으면 수정).
3. 사용자 확정 후 SSOT 반영은 사용자가 직접(Claude는 SSOT 직접 수정 금지, 커널 스펙과 동일한 패치 문서 방식으로 준비).
